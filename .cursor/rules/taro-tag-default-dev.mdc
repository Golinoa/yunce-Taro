---
description: Taro 推远程/打标签默认 dev-*，不上传体验版
alwaysApply: true
---

# Taro 推送与打标签（默认不发体验版）

用户说「推 GitHub / 打标签 / 推远程 / 跑 CI」且**未明确要求发体验版**时：

| 默认 | 禁止（除非用户强调） |
|------|----------------------|
| 推 `main` + 打并推送 **`dev-*`**（或 `ci-*`） | 打 **`v*`** |
| 只触发 `ci.yml`（check + coverage + `build:weapp:dev`） | 触发 `release.yml` |
| 本地/CI **不**跑 `npm run upload:weapp` | 上传微信开发版/设体验版 |

## 何时才打 `v*` / 上传

仅当用户**明确**说出下列之一（或等价口令）时才走发版上传：

- 「发体验版」「上传微信」「设为体验版」
- 「打 `v*` / 正式发版 / release 上传」
- 明确指定版本号如 `v1.0.4` **且**要求上传小程序

模糊的「推一下」「打个标签」「发版给测试看看」→ **一律 `dev-*`，不上传**。

## 标签示例

```bash
# 默认（仅 CI）
git tag dev-membership-orders-1
git push origin HEAD
git push origin dev-membership-orders-1

# 仅当用户强调体验版
git tag v1.0.4-membership-orders
git push origin v1.0.4-membership-orders   # → release.yml 构建 prod + upload
```

推送前仍须按仓库规则跑 `npm run verify:sop`（完整门禁）。
