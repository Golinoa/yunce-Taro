---
description: 松果排课小程序开发入口与真实 API 联调约束
alwaysApply: true
---

# 小程序开发入口

先读 AGENTS.md 与 docs/README.md，再按统一模块指南定位代码。详细样式、表单、滚动与组件约束继续使用 Agents/ 对应文件，不复制另一套规则。

- UI 固定，后端优先兼容前端；产品冲突先记录并确认。
- 数据走真实 Service/request；src/data 已删除，不恢复运行时 Mock。
- 改运行代码执行 npm run check（至少 typecheck）及 npm run build:weapp:dev。
- 只改文档不运行业务构建，不上传微信、不打发布标签。
