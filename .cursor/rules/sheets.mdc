---
description: 云策教务弹窗规范 — BottomSheet 单 visible prop、所有业务弹窗封装为独立 Sheet。写弹窗时强制遵守。
globs: **/*Sheet*/**/*.tsx
alwaysApply: false
---

# 弹窗开发规范

## 1. BottomSheet 基础组件

### 唯一允许的弹窗根容器

所有弹窗必须用 `<BottomSheet>`，禁止在页面内联 `fixed inset-0 z-200` 蒙层。

```tsx
import BottomSheet from '@/components/BottomSheet';

<BottomSheet
  visible={showSheet}        // ✅ 单 prop 模式
  title="标题"
  onClose={() => setShowSheet(false)}
  height="70vh"              // 可选：固定高度（默认 70vh），传 'auto' 自适应
>
  <View className="px-4 pb-6">
    {/* 业务内容 */}
  </View>
</BottomSheet>
```

### Props

| Prop | 类型 | 必填 | 说明 |
|---|---|---|---|
| `visible` | `boolean` | ✅ | 控制渲染 + 动画（**唯一推荐 prop**） |
| `title` | `string` | ❌ | 标题栏文字 |
| `onClose` | `() => void` | ❌ | 关闭回调（点击遮罩也会触发） |
| `height` | `string` | ❌ | 内容面板高度，默认 `70vh`，`'auto'` 自适应 |
| `className` | `string` | ❌ | 内容区额外类名 |

### ❌ 禁止写法

```tsx
// ❌ 双 prop 模式（已废弃）
<BottomSheet show={showSheet} visible={showSheet} />

// ❌ 页面内联弹窗
{showSheet && (
  <View className="fixed inset-0 z-200">
    <View className="bg-black/50" onClick={...} />
    <View className="absolute bottom-0 ...">{/* 手写弹窗 */}</View>
  </View>
)}

// ❌ 用 Taro.showModal 替代业务弹窗（仅紧急系统告警可用）
Taro.showModal({ title: '确认删除？' });  // ❌ 应封装 ConfirmDialog
```

## 2. 业务 Sheet 强制封装

**每个业务弹窗必须独立成一个组件**，按模块目录归类：

```
src/components/teacher/AddTeacherSheet/index.tsx
src/components/teacher/EditTeacherSheet/index.tsx
src/components/teacher/PayConfirmSheet/index.tsx
src/components/lead/ConvertSheet/index.tsx
src/components/lead/FollowUpSheet/index.tsx
src/components/booking/BookingControlSheet/index.tsx
...
```

### 标准模板

```tsx
import React, { useState, useEffect } from 'react';
import { View, Text } from '@tarojs/components';
import BottomSheet from '@/components/BottomSheet';
import FormInput from '@/components/FormInput';
import cn from 'classnames';

export interface XxxSheetProps {
  /** 是否可见 */
  visible: boolean;
  /** 业务数据（可空） */
  target?: SomeType | null;
  /** 确认回调 */
  onConfirm: (payload: SomePayload) => void;
  /** 关闭回调 */
  onClose: () => void;
}

/**
 * XxxSheet - 功能简述
 *
 * 使用场景：在哪个页面、什么操作触发
 * 功能：具体功能描述
 * 相关组件：关联的其他 Sheet
 */
const XxxSheet: React.FC<XxxSheetProps> = ({
  visible,
  target,
  onConfirm,
  onClose,
}) => {
  const [form, setForm] = useState({ name: '', amount: 0 });

  // 打开时重置内部表单
  useEffect(() => {
    if (visible) {
      setForm({ name: '', amount: 0 });
    }
  }, [visible]);

  const handleConfirm = () => {
    if (!form.name) {
      Taro.showToast({ title: '请输入名称', icon: 'none' });
      return;
    }
    onConfirm(form);
  };

  return (
    <BottomSheet visible={visible} title="标题" onClose={onClose}>
      <View className="px-4 pb-6">
        {/* 内容区 */}
        <FormInput
          label="名称"
          value={form.name}
          onInput={(e) => setForm((f) => ({ ...f, name: e.detail.value }))}
        />

        {/* 按钮区 */}
        <View className="flex gap-3 mt-6">
          <View
            className="flex-1 py-3 rounded-xl text-center text-sm font-semibold bg-muted text-muted-foreground"
            onClick={onClose}
          >
            取消
          </View>
          <View
            className="flex-1 py-3 rounded-xl text-center text-sm font-semibold bg-primary text-white"
            onClick={handleConfirm}
          >
            确认
          </View>
        </View>
      </View>
    </BottomSheet>
  );
};

export default XxxSheet;
```

## 3. 页面侧状态管理

```tsx
// 页面中管理弹窗状态
const [showXxxSheet, setShowXxxSheet] = useState(false);
const [xxxTarget, setXxxTarget] = useState<SomeType | null>(null);

// 打开（携带上下文）
const handleOpenXxx = (target: SomeType) => {
  setXxxTarget(target);
  setShowXxxSheet(true);
};

// 关闭（清理上下文）
const handleCloseXxx = () => {
  setShowXxxSheet(false);
  setXxxTarget(null);
};

// 确认（执行业务）
const handleConfirmXxx = async (payload: SomePayload) => {
  try {
    await teacherService.add(payload);
    Taro.showToast({ title: '添加成功', icon: 'success' });
    handleCloseXxx();
    // 刷新列表
  } catch (err) {
    Taro.showToast({ title: '操作失败', icon: 'none' });
  }
};

// 渲染
<XxxSheet
  visible={showXxxSheet}
  target={xxxTarget}
  onConfirm={handleConfirmXxx}
  onClose={handleCloseXxx}
/>
```

## 4. 样式规范

### 内容区

```tsx
// 统一 padding
<View className="px-4 pb-6">
```

### 按钮

```tsx
// 双按钮（取消 + 确认）
<View className="flex gap-3">
  <View className="flex-1 py-3 rounded-xl text-center text-sm font-semibold bg-muted text-muted-foreground" onClick={onClose}>
    取消
  </View>
  <View className="flex-1 py-3 rounded-xl text-center text-sm font-semibold bg-primary text-white" onClick={handleConfirm}>
    确认
  </View>
</View>

// 单按钮（主操作）
<View className="py-[28rpx] rounded-2xl text-center text-base font-semibold text-white bg-gradient-primary" onClick={handleConfirm}>
  确认
</View>
```

### 提示信息

```tsx
// 警告提示
<View className="bg-amber-bg rounded-xl p-4 mb-4">
  <Text className="text-sm text-foreground">提示内容</Text>
</View>

// 危险提示
<View className="bg-destructive-5 rounded-xl p-4 mb-4">
  <Text className="text-sm text-destructive">警告内容</Text>
</View>
```

### 输入框

- **单行输入**：`<FormInput variant="default" />`（默认）
- **胶囊搜索**：`variant="capsule"`
- **行内表单**：`variant="ghost"`
- **多行文本**：`<Textarea className="w-full bg-muted rounded-xl p-3 text-sm min-h-[120rpx] text-foreground" />`

## 5. 已有 Sheet 组件清单（写新弹窗前先查）

| 组件 | 路径 | 用途 |
|---|---|---|
| `AddTeacherSheet` / `EditTeacherSheet` | `components/teacher/` | 教师增改 |
| `DeductionSheet` | `components/teacher/` | 扣款/补发 |
| `ConfirmSalarySheet` / `PayConfirmSheet` | `components/teacher/` | 薪资确认/发放 |
| `ResignSheet` / `SalaryModelSheet` / `PaymentSettingsSheet` | `components/teacher/` | 离职/薪资模型/发放设置 |
| `ConvertSheet` / `FollowUpSheet` | `components/lead/` | 线索转化/跟进 |
| `BookTrialByClassSheet` | `components/lead/` | 按班级预约试听 |
| `MemberActionSheet` | `components/student/` | 学员操作 |
| `BookingControlSheet` | `components/booking/` | 预约控制 |
| `LessonPreviewSheet` | `components/lesson/` | 课程预览 |
| `PackageSelectSheet` / `StudentSelectSheet` | `components/package/` | 套餐/学员选择 |
| `ProxyUserSelectSheet` | `components/proxy-booking/` | 代预约选人 |
| `AlertSheet` / `DateRangeSheet` | `components/statistics/` | 预警/日期范围 |
| `CampusSelectSheet` | `components/home/` | 校区选择 |
| `RoleSwitchSheet` | `components/RoleSwitchSheet/` | 角色切换 |
| `LoginDecisionDialog` / `LoginFlowPopover` / `LoginHelpDialog` / `LoginIssueSheet` / `AccountLoginSheet` | 登录模块 | 登录流程 |
| `AgreementSheet` / `AgreementDialog` | — | 协议 |

## 6. 自检

- [ ] 用 `BottomSheet` 封装，未内联 `fixed inset-0` 蒙层
- [ ] 单 `visible` prop，未用 `show + visible` 双 prop
- [ ] 独立 Sheet 组件，按模块目录归类
- [ ] `export interface XxxSheetProps` + `export default XxxSheet`
- [ ] JSDoc 包含使用场景 + 功能
- [ ] 页面侧用 `useState` 管理 `showXxxSheet` + `xxxTarget`
- [ ] 打开时重置内部表单（`useEffect` 监听 `visible`）
- [ ] 错误处理走 `Taro.showToast`，不要抛 `alert`
- [ ] 样式纯 UnoCSS + rpx + Token