---
description: 云策教务样式规范 — UnoCSS 原子化 + rpx + Design Token。编辑样式相关代码前必读。
globs: **/*.{tsx,ts,scss,css}
alwaysApply: true
---

# 样式规范（UnoCSS + rpx + Token）

## 1. 三条铁律

1. **禁止创建 SCSS 文件** — 一律 UnoCSS；遗留 `src/styles/` 目录文件待迁移后删除。
2. **禁止内联 `style`** — 除非值是动态计算（ECharts 配置、动画进度、定位坐标）。所有静态样式必须用 UnoCSS 类名或 Token 规则。
3. **尺寸统一 rpx** — 写 `text-[28rpx]`、`p-[20rpx]`，**禁止** `px`、`rem`。`presetRemRpx({ baseFontSize: 14, screenWidth: 375 })` 已自动转换。

## 2. Design Token 单一数据源

- **源头**：`src/theme.ts`（颜色/间距/圆角/字号/阴影/动画/渐变）
- **CSS 变量落地**：`src/app.scss`（修改 `theme.ts` 必须同步 `app.scss`）
- **JS 运行时**：`import { hsl, hexColors } from '@/theme'`（hexColors 仅用于 ECharts 等无法用 CSS 变量的场景）
- **UnoCSS 引用**：通过 `hsl(var(--primary))` 间接引用

## 3. Token 引用方式（按场景）

| 场景 | 写法 | 示例 |
|---|---|---|
| 颜色（主） | `text-{token}` / `bg-{token}` / `border-{token}` | `text-primary`、`bg-card`、`border-border` |
| 颜色（半透明） | UnoCSS rules | `bg-primary-10`、`bg-destructive-5`、`bg-amber-bg` |
| 颜色（角色） | 专用 token | `text-rolePrincipal`、`bg-roleTeacher` |
| 渐变背景 | rules 快捷 | `bg-gradient-primary`、`bg-gradient-accent`、`bg-petal-blue` |
| 阴影 | shortcuts | `shadow-soft`、`shadow-card`、`shadow-float` |
| 圆角 | theme | `rounded-xl`、`rounded-2xl`、`rounded-b-60rpx` |
| 间距 | 原子类 | `p-3`、`px-4`、`mb-6`、`gap-2` |
| 安全区 | rules | `pt-safe`、`pt-nav-safe`、`pb-safe-bottom` |
| 交互反馈 | shortcuts | `press-scale`、`press-bg`、`active:opacity-70` |
| 滚动条隐藏 | `scrollbar-hide` | — |
| 文本截断 | `line-clamp-2` / `line-clamp-3` | — |

## 4. ❌ 禁止写法

```tsx
// ❌ 硬编码色值
<View style={{ background: '#f5faf8' }} />
<Text className="text-[#D94040]" />

// ❌ 内联 style（非动态值）
<View style={{ border: '3rpx solid #D5E8E0', backgroundColor: '#f5faf8' }} />

// ❌ px/rem 单位
<Text className="text-[14px]" />
<View className="text-base leading-[1.5]" />

// ❌ SCSS 文件 + import
import './index.scss';

// ❌ CSS 伪元素 ::before / ::after 实现装饰（小程序不支持大部分伪元素）
// ❌ 模板字符串拼 className
<View className={`card ${active ? 'is-active' : ''}`} />
```

## 5. ✅ 正确写法

```tsx
// ✅ Token 颜色
<View className="bg-muted" />
<Text className="text-destructive" />

// ✅ UnoCSS shortcuts/rules（复杂样式）
<View className="form-input-wrap" />
<View className="bg-primary-10" />

// ✅ rpx 单位
<Text className="text-[28rpx]" />

// ✅ classnames 库
import cn from 'classnames';
<View className={cn('card', { 'is-active': active })} />

// ✅ 用 View 元素替代伪元素
<View className="absolute bottom-0 left-1/2 -translate-x-1/2 w-[40rpx] h-[6rpx] rounded-[4rpx] bg-primary" />

// ✅ 动态值例外（必须内联 style）
<View style={{ height: `${progress}%`, background: hexColors.primary }} />
<ECharts option={{ color: [hexColors.chart1, hexColors.chart2] }} />
```

## 6. 新增样式的决策流程

```
1. uno.config.ts shortcuts 是否已有？
   ├─ 是 → 直接使用
   └─ 否 → 是否跨组件复用？
       ├─ 复用 → 在 uno.config.ts 的 rules/shortcuts 中新增
       └─ 单次 → 内联 UnoCSS 原子类，不新增规则
```

### rules vs shortcuts 选择

| 类型 | 用途 | 示例 |
|---|---|---|
| `rules` | 需要特殊 CSS 属性（渐变、阴影、半透明、安全区） | `bg-gradient-primary`、`bg-primary-10`、`pt-safe` |
| `shortcuts` | 组合多个原子类（按钮、标签、状态） | `btn-primary`、`chip-active`、`press-scale` |

## 7. 颜色与状态映射速查

- 主色：`primary` (#3B6EF5 蓝)
- 强调色：`accent` (#8B5CF6 紫)
- 成功：`success` (#10b981)
- 警告：`warning` (#F59E0B 琥珀)
- 错误：`destructive` / `error` (#ef4444)
- 信息：`info` (#0EA5E9)
- 角色：校长 `rolePrincipal` (琥珀)、教师 `roleTeacher` (蓝)、家长 `roleParent` (紫)
- 文本：`foreground` / `foregroundSecondary` / `mutedForeground`
- 背景：`background` / `card` / `muted`
- 边框：`border` / `borderLight`

## 8. 样式迁移检查清单（SCSS → UnoCSS）

- [ ] 所有类名替换为 UnoCSS 原子类或 shortcuts
- [ ] 硬编码色值替换为 Token
- [ ] `px` 全部替换为 `rpx`
- [ ] `::before`/`::after` 替换为 `<View />`
- [ ] 复杂样式提取到 `uno.config.ts` rules/shortcuts
- [ ] 删除 SCSS 文件与 `import './xxx.scss'`
- [ ] 编译验证无新增错误（`npm run check`）