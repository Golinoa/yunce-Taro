# 待办四象限（事态等级）— 接口契约

> **已并入完整待办契约**：请以 [`08-todo-module-api-contract.md`](./08-todo-module-api-contract.md) 为准。  
> 四象限路径：`PUT /todos/:todoId/quadrant`（不再使用 `/home/teacher/todos/...`）。  
> 前端入口：`todoService.updateQuadrant`（`src/services/todo.ts`）。

本文保留作历史索引；新联调请只看 08 文档。
