# 云策教务 · 代码审查标准与流程（Code Review Standard & Process）

> 版本：v1.0 ｜ 适用范围：`yunceTaro` 全部前端（Taro 3 + React 18 + TypeScript 严格模式）
> 配套规范：`AGENTS.md`、 `Agents/*.md`（组件/样式/Service/状态/表单/弹窗/审查细则）
> 本文在既有「工程化铁律」之上，**补上角色分工、审查时机、跨模块/跨服务数据一致性专项**，形成可落地的闭环。

---

## 0. 文档目的

团队代码质量参差不齐，根因不是「不会写」，而是「**没有统一的门禁与一致的认知**」。本文解决三件事：

1. **谁来审、审什么** —— 明确角色分工与责任边界（RACI）。
2. **什么时候审** —— 提交前 / 提交时 / 合并前 / 发布前四道关卡，层层设防。
3. **怎么算审过** —— 维度化清单 + 数据一致性专项 + 验证流程， reviewer 照单打勾即可。

---

## 1. 审查角色分工（RACI）

| 角色 | 英文 | 主要职责 | 责任类型 |
|------|------|----------|----------|
| 开发者 | Author | 自检、写单测/走查、回应评审意见、修复 | **R**（执行） |
| 评审人 | Reviewer | 对可读性/规范/逻辑正确性负责，给出 🔴🟡💭 结论 | **R/A**（复核） |
| 模块 Owner | Module Owner | 对本模块关键链路（薪资、课表、报名等）做领域把关 | **A**（领域负责） |
| 技术负责人 | TL | 仲裁争议、把控架构与跨模块影响、抽查发布前门禁 | **A**（最终负责） |
| 安全/架构专员 | Sec/Arch | 仅在大改/涉及鉴权/外部接口时介入，非每 PR 必到 | **C**（咨询） |

**RACI 说明**：R=负责执行，A=最终 accountable，C=被咨询，I=被知会。

### 评审人资格与配比

- 普通 PR：**≥1 名 Reviewer**（非 Author 本人）。
- 关键链路 PR（薪资状态机、课表/排课、报名/分班、任何写操作与跨 Store 改动）：**Reviewer + 对应模块 Owner 双签**。
- Reviewer 不得连续 approve 同一 Author 超过 **5 个 PR** 而不换人，避免「熟人互点」。
- 单 PR 代码量建议 **≤400 行**；超过则要求拆 PR，否则 Reviewer 可退回。

### 责任边界

- **Author 对「能不能跑、有没有自测」负全责**；Reviewer 不替 Author 做冒烟测试。
- **Reviewer 对「该发现而没发现的问题」负责**；标注 💭 的项不阻塞合并，但需在 3 个工作日内跟进。
- **模块 Owner 对「领域规则被破坏」负责**（如薪资被允许反向流转）。

---

## 2. 审查时机与流程（四道关卡）

```
开发者写码 ──▶ [Gate 0] 提交前自检    ──▶ git commit
                (Author 自填清单)            │ husky 触发
                                             ▼
                                      [Gate 1] 提交时门禁
                                      (lint-staged + typecheck)
                                             │ 失败则拒绝提交
                                             ▼
                                      推送 / 开 PR
                                             │
                                             ▼
                                      [Gate 2] 合并前 PR 评审
                                      (Reviewer + Owner 双签)
                                             │ 通过
                                             ▼
                                      合并到主分支
                                             │
                                             ▼
                                      [Gate 3] 发布前专项
                              (TL 抽查 + 数据一致性回归走查)
```

### Gate 0 — 提交前自检（Author，提交前 5 分钟）

Author 在 `git commit` 前，对照本文 §3 / §4 清单逐项自答。可借助 `npm run check` 一键跑门禁：

```bash
npm run check          # typecheck + lint + format 全量
```

提交信息遵循 `Agents/review.md` 约定：`feat:` / `fix:` / `refactor:` / `style:` / `chore:`。

### Gate 1 — 提交时自动门禁（husky + lint-staged，强制）

- 暂存文件自动 `eslint --fix` + `prettier --write`，有 error 则**拒绝提交**。
- Author 无法用 `--no-verify` 绕过（CI 侧再次校验）。
- 此门禁只管**格式与静态规则**，不管**逻辑与业务正确性**——那是 Gate 2 的事。

### Gate 2 — 合并前 PR 评审（Reviewer / Owner，强制，阻塞合并）

- 每个 PR 必须至少有一份 **Approval**；关键链路需 **双签**。
- Reviewer 使用 §3 清单逐维审查，使用 §4 专项检查数据一致性。
- 评论标注规范（沿用火眼眼专家风格）：

  | 标记 | 级别 | 含义 | 是否阻塞合并 |
  |------|------|------|--------------|
  | 🔴 | Blocker | 正确性/安全/数据一致性/崩溃风险 | **是** |
  | 🟡 | Suggestion | 可读性/性能/缺失校验/缺测试 | 建议修，可协商 |
  | 💭 | Nit | 命名/注释/风格细节 | 否 |

- 「请求改动（Changes Requested）」后，Author 修复并回复「已修复：<行号>」，Reviewer 复核闭环。
- **禁止**在评论未解决时强行 merge；TL 对强行 merge 负责。

### Gate 3 — 发布前专项（TL + 数据一致性回归，强制）

- 每个版本（提审/发版）前，TL 抽取 **2~3 个关键链路 PR** 做架构与一致性复审。
- 跑 §4.4《一致性回归检查表》全部场景，确认跨模块/跨服务数据同步无异常。
- 仅 Mock 阶段也须跑（验证内存数据与各 Store 副本一致）；联调后追加真实接口幂等/重试验证。

---

## 3. 审查清单（维度化）

> 每项 Reviewer 打勾「通过 / 有问题（附行号）」。以下清单为 `Agents/review.md` 的超集，新增维度以 ⭐ 标注。

### 3.1 代码规范（对齐 AGENTS.md 工程化铁律）

- [ ] 技术栈未越界：无类组件、无 Vue、无 moment.js、无 Redux/MobX
- [ ] 样式：`UnoCSS` 原子类，无新建 SCSS，无内联 `style`（动态值除外），单位 `rpx` 无 `px/rem`
- [ ] 颜色/尺寸使用设计 Token（`text-primary`/`bg-card`/`border-border`），无硬编码色值
- [ ] 组件复用：先查 `src/components/`；弹窗用 `BottomSheet` 且只传 `visible`；输入框用 `FormInput`
- [ ] `PickerView` 的 `indicator-style` 高度为 **px**（非 rpx），已 `grep -rn 'indicatorStyle' src/` 确认
- [ ] TypeScript：Props 接口导出、无隐式 `any`、无 `@ts-ignore`/`as any`、无未用导入
- [ ] 常量 `UPPER_SNAKE_CASE` 提取；事件 `handle` 前缀；`useCallback/useMemo` 依赖完整
- [ ] 组件含 JSDoc（使用场景 + 功能）

### 3.2 可读性与设计 ⭐

- [ ] 单一职责：函数/组件只做一件事，长度可控（建议函数 ≤80 行）
- [ ] 命名自解释：`confirmSalary` 而非 `doStuff`；布尔用 `is/has/can` 前缀
- [ ] 无「魔法数字/字符串」，枚举值集中定义（如薪资状态 `SalaryStatus` 联合类型）
- [ ] 控制流扁平：早返回（guard clause）优于深层嵌套；无 `if/else` 大块堆叠
- [ ] 复杂业务逻辑有简短注释说明「为什么」，而非「做了什么」
- [ ] 无大段重复代码，已抽取公共函数/组件（DRY）

### 3.3 安全性 ⭐

- [ ] 用户输入经 `FormInput` 受控，无直接拼接进 `className`/`url`/SQL 风险点
- [ ] 外部数据（接口/路由参数/二维码）使用前做**类型与边界校验**，不信任后端
- [ ] 鉴权：需要登录的接口/页面有登录态判断；敏感操作（薪资确认/支付）有权限/二次确认
- [ ] 无硬编码密钥、token、内部地址；密钥走环境变量
- [ ] 小程序：`web-view`/`navigateTo` 的 URL 白名单校验，防任意跳转
- [ ] 日志不打印手机号、身份证、密码等敏感字段

### 3.4 性能 ⭐

- [ ] 列表/大数组用 `useMemo` 缓存派生值；避免在 render 中 `Array.map` 做重计算
- [ ] Zustand **选择性订阅**（`useXStore(s => s.field)`），未整店订阅
- [ ] 避免 N+1：列表项不各自发请求拉详情；批量操作用 `batchXxx`
- [ ] 图片/资源走合适尺寸与懒加载；无同步大循环阻塞主线程
- [ ] 事件回调 `useCallback` 包裹，依赖完整，避免子组件无谓重渲染
- [ ] 定时器/`Subscription` 在 `useEffect` 清理，无内存泄漏

### 3.5 错误处理与健壮性 ⭐

- [ ] Service 层只抛错不弹 UI；页面层 `try/catch` 处理，按 `ApiError` 区分提示
- [ ] 异步操作有 loading 态与失败兜底（toast/空态/重试入口）
- [ ] 空数据/异常网络有**空态与错误态** UI，不白屏、不崩溃
- [ ] 边界：空数组、null、超长文本、0、负数（如薪资/人数）均有合理处理
- [ ] 写操作有**乐观更新 + 失败回滚**或**先成功再刷新**策略（见 §4.2.4）
- [ ] 无吞异常（`catch` 空块）；错误上报/日志可定位

### 3.6 测试与验证 ⭐

- [ ] 关键纯函数（状态机流转、金额计算、日期区间）有单测或至少手动走查记录
- [ ] 涉及写操作的 PR，Author 在 PR 描述附**自测步骤与截图**
- [ ] Mock 阶段已用 `VITE_USE_MOCK=true` 真机/模拟器跑通；联调后附真实接口验证
- [ ] 无「我本地能跑」式交付，给出可复现验证路径

### 3.7 小程序专项 ⭐

- [ ] 编译遵守铁律：`$env:VITE_USE_MOCK="true"; npm run build:weapp`（或 `:clean`）
- [ ] 无使用浏览器特有 API（`window`/`document`/`localStorage`）；用 `Taro.*` 等价物
- [ ] 路由参数超限（小程序 `navigateTo` 2MB/字符限制）已规避，大对象走 Store/全局
- [ ] 生命周期正确使用（`useDidShow` 处理返回刷新，`useUnload` 清理）

---

## 4. 数据一致性专项（核心，跨模块 / 跨服务）

> 本项目最大风险不在单文件，而在**「同一份业务数据在多处持有副本，改动时未同步」**。
> 当前为 Mock 内存层，未来切 API 后跨服务一致性更关键。本节给出**专项检查项 + 验证流程**。

### 4.1 风险模型（本项目的典型不一致场景）

| 风险 | 触发场景 | 后果 |
|------|----------|------|
| 多副本失同步 | teacher 同时存在于 `teacherStore` 与 `classStore` 引用；改一处另一处旧 | 列表/详情/仪表盘数字对不上 |
| 列表/详情缓存分离 | 详情页编辑后返回，列表未刷新 | 看到旧数据 |
| 状态机被反向 | 薪资 `paid → confirmed` 被允许 | 业务规则被破坏 |
| 乐观更新未回滚 | 确认薪资接口失败但 UI 已置 `confirmed` | 用户以为成功实则失败 |
| Mock 引用共享 | `mockGetTeachers()` 返回同一数组引用，某处浅拷贝后两端漂移 | 内存数据与实际展示不一致 |
| 跨服务（未来） | 薪资服务与考勤服务各自写，无事务/补偿 | 总额与明细不符 |
| 派生数据未重算 | 仪表盘「待确认数」= 列表 filter，列表更新但仪表盘用的是旧副本 | 首页数字失真 |

### 4.2 专项检查项（Reviewer 必查）

#### 4.2.1 单一数据源（SSOT）⭐

- [ ] 每个业务实体（teacher/student/class/salary…）有**唯一权威来源**（Store 或 Service），页面不直接持有可变副本
- [ ] 跨 Store 引用同一实体时，采用 **id 引用 + 各自按需从权威源取数**，而非互相拷贝对象
- [ ] 无「A 组件改了 state.A，B 组件却用自己 props 里的旧值」式拷贝

#### 4.2.2 状态机完整性 ⭐（薪资 `pending→confirmed→paid`）

- [ ] 状态流转**仅允许单向**：`pending→confirmed→paid`，无反向、无跳跃
- [ ] 流转集中在 Store action 或 Service 方法内，**不在组件里散点 `set`**
- [ ] 非法流转被拦截并提示（如已 `paid` 再点确认应禁用/报错）
- [ ] 状态枚举用联合类型约束，新增状态需同步更新类型与流转表

```ts
// ✅ 状态机集中在 action，附断言
confirmSalary: (id) => set((s) => ({
  teachers: s.teachers.map((t) => {
    if (t.id !== id) return t;
    if (t.salaryStatus !== 'pending') return t; // 非 pending 不允许流转
    return { ...t, salaryStatus: 'confirmed' };
  }),
})),
```

#### 4.2.3 列表 / 详情 / 缓存同步 ⭐

- [ ] 写操作（add/update/remove）成功后，**列表与详情数据源同步刷新**（从 Service 重新拉取或 action 内更新同一引用）
- [ ] 页面 `useDidShow` 对「可能被别的页面改过」的数据做刷新，避免返回看旧值
- [ ] 删除/批量操作后，相关列表/计数（如「待确认数」）同步递减

#### 4.2.4 乐观更新与回滚 ⭐

- [ ] 乐观更新必须有**回滚路径**：`try { set optimistic; await api } catch { rollback; toast }`
- [ ] Mock 阶段至少保证「失败分支被走到」（可注入 mock 失败验证回滚）
- [ ] 不允许「先 set 成功、接口失败却不还原」的静默错误

#### 4.2.5 跨 Store 同步 ⭐

- [ ] 一个实体的改动若影响多个 Store（如删 teacher 影响 class 的成员列表），有**统一协调点**（在 action 内跨 Store `getState()` 更新，或事件总线）
- [ ] 无「两个 Store 各自维护同一关系」导致双份真相

#### 4.2.6 跨服务一致性（联调/生产阶段）⭐

- [ ] 写操作**幂等**：重复提交（用户双击/重试）不会产生双倍记录 → 用请求幂等键 / 状态前置校验
- [ ] 跨服务写有**补偿/最终一致**预案（如薪资确认后通知考勤，失败可重试且可查）
- [ ] 接口有**版本/ETag/乐观锁**，并发编辑不产生丢失更新
- [ ] 批量操作要么全成要么可感知部分失败，无「静默半成功」

#### 4.2.7 Mock 数据引用一致性 ⭐

- [ ] `mockXxx()` 返回**新副本**（`[...MOCK]`）或明确文档化「返回引用、调用方不得原地改」
- [ ] Store 接收到的 mock 数据不被原地 mutate，统一经 action 的不可变更新
- [ ] 多 mock 函数共享同一内存数组时，任一写操作对全局可见且一致

### 4.3 验证流程（Reviewer 如何确认一致性）

**静态核对（PR 评审时）**：
1. 定位本 PR 改动的**实体与涉及 Store/Service**（`grep` 相关 `useXStore` / `xxxService`）。
2. 对照 §4.2 七项逐条打勾，凡涉及写/跨 Store 改动须有对应同步证据（代码行号）。

**运行时走查（关键链路 PR 必做，Author 附证据，Reviewer 抽验）**：
1. 打开涉及页面 → 记录某条数据的「基准值」（如某教师薪资状态、首页待确认数）。
2. 执行写操作（确认/编辑/删除/分班）→ 返回上一页/切到相关页。
3. 核验：**列表、详情、仪表盘、关联模块**四处展示是否全部同步更新。
4. 强制失败路径（Mock 注入异常 / 断网）→ 验证回滚与错误提示，确认无「假成功」。

**数据一致性测试场景库（建议沉淀为用例，回归时复用）**：

| 编号 | 场景 | 预期 |
|------|------|------|
| DC-01 | 确认薪资 → 返回首页 | 待确认数 -1，状态变 confirmed |
| DC-02 | 编辑教师信息 → 返回列表 | 列表对应行立即反映新值 |
| DC-03 | 删除教师 → 相关班级页 | 班级成员列表移除该教师，人数 -1 |
| DC-04 | 分班/调班 → 学员与班级双向 | 学员班级字段与班级成员列表一致 |
| DC-05 | 课表变更 → 教师/学员课表 | 双方课表同步显示变更 |
| DC-06 | 薪资 paid 再点确认 | 按钮禁用/提示，状态不反向 |
| DC-07 | 写操作接口失败 | UI 回滚到操作前，toast 报错 |
| DC-08 | 双击提交（幂等） | 仅产生一条记录 |

### 4.4 一致性回归检查表（Gate 3 发布前必跑，全绿方可发版）

- [ ] DC-01 ~ DC-08 全部走通（Mock 阶段）
- [ ] 联调后追加：真实接口下 DC-01/04/07 与幂等（DC-08）验证
- [ ] 首页/仪表盘所有「计数类」派生数据，在对应写操作后已重算
- [ ] 无 Store 持有「过期整表副本」超过一个用户操作周期
- [ ] 状态机全链路无反向/跳跃，类型枚举与实际值一致

---

## 5. 审查度量与持续改进

| 指标 | 目标 | 说明 |
|------|------|------|
| 评审覆盖率 | 100% PR 有 Approval | Gate 2 强制 |
| 缺陷逃逸率 | 逐月下降 | 生产/提审发现的问题回溯到哪道 Gate 漏了 |
| 平均评审时长 | ≤ 1 个工作日 | 避免 PR 堆积 |
| 清单打勾率 | Reviewer 逐项留痕 | 禁止「LGTM」式空评 |

- 每月 TL 抽取 **3 个逃逸缺陷** 做根因复盘，更新本文清单（版本号 +1）。
- 清单项若连续 3 个月无人触发，降级为 💭 或移出，保持清单「有用不臃肿」。

---

## 6. 附录

### A. 合并请求（PR/MR）模板（复制即用）

```markdown
## 变更说明
> 一句话 + 关键改动点

## 关联模块 / 实体
> teacher / student / class / salary / 跨模块：___

## 审查自检（Gate 0）
- [ ] 已跑 `npm run check` 全绿
- [ ] 已对照 §3 清单（规范/可读/安全/性能/错误处理）
- [ ] 涉及写/跨 Store：已对照 §4.2 数据一致性七项
- [ ] 附自测步骤与截图

## 数据一致性影响（如涉及写/跨模块）
- 受影响 Store/Service：___
- 同步策略（刷新/回滚/双签）：___
- DC-0x 场景走查结果：___

## 风险与回滚
> 最坏情况 + 回滚方式
```

### B. 审查评论标注规范

- 🔴 **Blocker**：正确性/安全/数据一致性/崩溃，必须修，阻塞合并
- 🟡 **Suggestion**：可读性/性能/缺校验/缺测试，建议修
- 💭 **Nit**：命名/注释/风格，可不改

格式示例：
```
🔴 安全性：薪资确认缺少权限二次确认
行 42：confirmSalary 直接执行，未校验当前用户是否有权操作。
建议：在 action 前置角色校验，越权时拒绝并 toast。
```

### C. 一键检查命令

```bash
npm run check            # typecheck + lint + format
npm run typecheck        # 仅类型
npm run lint             # 仅 lint
grep -rn 'indicatorStyle' src/   # 确认 PickerView 高度为 px
```

### D. 快速参考卡（速查）

| 关卡 | 谁 | 卡什么 | 阻塞？ |
|------|----|--------|--------|
| Gate 0 提交前 | Author | 自检清单 + `npm run check` | 自卡 |
| Gate 1 提交时 | husky | lint/format/type | 是 |
| Gate 2 合并前 | Reviewer+Owner | §3 + §4 双签 | 是 |
| Gate 3 发布前 | TL | §4.4 回归走查 | 是 |

> 记住一句话：**格式交给机器，逻辑交给人，一致性交给专项。**
