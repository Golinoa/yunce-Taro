# 2026-08-22 工作日志

## 代码审查标准与流程文档
- 产出：`D:\Coding\yunce\yunceTaro\CODE_REVIEW_STANDARD.md`（v1.0）
- 内容：四道审查关卡（Gate0 提交前自检 / Gate1 husky 门禁 / Gate2 合并前 PR 双签 / Gate3 发布前一致性回归）、RACI 角色分工、维度化清单（规范/可读/安全/性能/错误处理/小程序）、**数据一致性专项**（SSOT、薪资状态机单向、列表-详情-缓存同步、乐观更新回滚、跨 Store 同步、跨服务幂等/补偿、Mock 引用一致性）+ DC-01~08 验证场景库 + 发布前回归检查表。
- 对齐既有 `AGENTS.md` 工程化铁律与 `Agents/review.md`；评论标注沿用 🔴🟡💭 火眼眼风格。
- 下一步（可选，未执行）：可在 `AGENTS.md` 第八节增加指向本标准的索引条目。

## 首轮代码审查（按 CODE_REVIEW_STANDARD 执行）
- 产出：`D:\Coding\yunce\yunceTaro\CODE_REVIEW_REPORT.md`
- 自动化门禁：`npm run typecheck` 全绿（0 error），类型层面干净。
- 结论：2 🔴（均数据一致性）+ 6 🟡 + 3 💭。
- 🔴 发现1：历史/未来月份 `mockGetTeachers(month)` 返回 `genMonthSnapshot`（由静态 `mockTeachers` 重生成，忽略内存 `_teachers`），写后刷新静默丢数据（`src/data/teacher.ts:781-802`，store `:284`）。
- 🔴 发现2：`confirmSalary` 用 `getList()`（忽略月份）而扣款类用 `getList(month)`，跨月视图错位/确认变 no-op（`src/stores/teacher.ts:208` vs `:284`）。
- 🟡 关键：getPendingCount 语义错（非 archived≠待确认，`teacher.ts:380`）；mockExecutePay 实发金额≠calcTotal（`teacher.ts:929` vs `:25`）；双教师模型薪资字段不进统一视图 `buildTeacherView`（`mock-database.ts:662-701`）；裸 `<Input>` 约40处；Store 写操作无 try/catch 回滚；SalaryStatus `teacher_confirmed` 悬空。
- 整改：🔴 合并前必修（统一月份数据源 + 修 getPendingCount）；🟡 排期（双模型重构、ESLint 收口裸 Input）。

## 首轮复审（Re-Audit）
- 产出：`D:\Coding\yunce\yunceTaro\CODE_REVIEW_REAUDIT.md`
- 方法：回读真实代码逐行核对 + 排查 UI 可达性/跨模块同步/状态机文档一致性/死代码。
- 裁定：**首轮无误报**；2 项需校准、补充 6 项遗漏。
  - 🔴1 机制修正：实际扣款走 `salary-adjust → updateTeacher(id,{deductions})`，**`addDeduction/updateDeduction/deleteDeduction` 是死代码**（全仓无页面调用）；且过去月被强制 `archived`、未来月被拦截，故"扣款立即消失"在当前 UI 不可达。根因（非当前月数据源非单一可信源）仍属实，定级 **🔴-latent**。
  - 🔴2 降级 🟡：确认/批量确认/发放/发送/增改教师等写后均 `getList()` 无月份（store :208/214/225/236/266/272/278），仅扣款类带 `salaryMonth`（:284/290/296）——不一致属实，但确认只在当前月可达，即时丢失不可达。
  - 新增遗漏（数据一致性专项本应覆盖）：A) `buildTeacherView` 把 `status` 硬编码 `'active'`（`mock-database.ts:672/692`）→ 离职状态不进统一视图；B) 新增教师 `campusIds:[]`；C) 状态机文档 `pending→confirmed→paid` 与实现 `pending→confirmed→sending→archived`+孤儿 `teacher_confirmed` 漂移；D) 应用内两个矛盾"待确认"计数（teacher-list 用错误 getPendingCount vs salary-payment 用正确 stats.pending）；E) 死代码扣款方法；F) 裸 Input 实为 ≈44 处（非约40）。
- 建议：标准 §4.2 增两条检查项「统一视图字段同步完整性」「状态机文档与实现一致性」。

## 全量逐模块代码审查（用户指令：重审全部、不跳读、画图、分 bug/链路）
- 产出：`D:\Coding\yunce\yunceTaro\CODE_REVIEW_FULL.md`（约 31KB，11 节 + 4 流程图 + 26 行问题表）。
- 方法：逐文件通读 stores(13)/services(22)/types(24)/data(26) + 核心逻辑文件 teacher.ts/mock-database.ts/students.ts/lead.ts；Grep 全仓扫断裂调用/字段冲突/双数据源/状态机漂移/时间基准分裂。已纠正一误判（消课链路实际可用，降级双类型风险）。
- 🔴 7 项（P0 必修）：L-08 调班 `mockTransferStudent` 空实现、L-09 结束班级 `mockEndClass` 空实现、L-02 统一教师视图硬 `status='active'`、L-03 双教师主数据同 ID 分裂、L-04 时间基准分裂(mock 2026-06 vs 真实时钟)、L-01 跨月 `genMonthSnapshot` 用静态 `mockTeachers` 忽略内存写入、B-01 `mockExecutePay` 实发算法≠`calcTotal` 展示。
- 🟡 约 18 项：B-05 线索转化 camelCase 脏数据、L-10 双 CoursePackage、L-05 状态机孤儿、L-06 扣款死链、B-02/B-03/B-04 计数/月份/下标、L-11 会员卡与课包不打通、L-12~L-18 待走查、Q-01 Store 无回滚、Q-02 裸 Input≈44。
- 致命点：调班/结束班级用户以为成功实际零改动；教师统一视图/薪资状态不同步；双类型/双主数据/时间基准三类结构性衔接不一致。P0 优先收口。

## 走查清单专项核验（L-11/L-12/L-13/L-14/L-17/L-18 代码级走查，写入 CODE_REVIEW_FULL.md 第十二节）
- L-11 会员卡↔课包：原"未打通"判定过时——代码有发卡/改卡联动(del L-11-A🔴单向只写不读，课后即失同步；L-11-B🟡 name.includes('会员卡')脆弱匹配；L-11-C🟡 remainingHours 可>totalHours 抹掉 usedHours)。
- L-12 临时调课：链路已实现(写storage→读→visible-schedules消费)；新 bug：覆盖键 `${sid}__${sourceDate}` 过窄致同排课多次调动双订(L-12-A🟡)；存储持久+基础排课内存易失错位(L-12-B🟡)。
- L-13 考勤/课时：主线一致(COURSE_PACKAGES as DB_PACKAGES 同数组)；风险：双扣减实现(mockCreateLessonRecord内联 + 独立 mockDeductPackageHours)漂移(L-13-A🟡)，purchasedHours 被误减(L-13-B🟡)。lesson-form 仅走 create 单扣，暂无双扣。
- L-14 线索锁定：weak→locked 写入正确集中(mockCreateLeadBooking L1295)；🔴 关键断裂：mockUpdateLead 用 Object.assign 无守卫，locked 线索归属可被任意改写(L-14-A)；卡片返回了状态但 UI 不区分(L-14-B)。
- L-17 统计：确认预警为硬编码假数据(MOCK_OPERATION_ALERTS/FINANCE_ALERTS 直返 services:175/219/224/312)L-17-A🔴；趋势半真(getVisibleLessonRecords实时 vs MONTHLY_STATS静态)L-17-B🟡。
- L-18 门店入驻：数据层 mockSubmitStoreEntry 调 campusService.add 立即建校区(del L-18-B🔴 与页面"1-3工作日审核"异步文案语义矛盾)；UI 提交后不刷新校区列表(del L-18-A🟡)。
- 走查后累计：新增/升级 4🔴(L-11/L-14/L-17/L-18)+2🟡(L-12/L-13)，全项目 🔴 累计 11 项。每项均附"优化建议 + 可度量验收标准(含 grep/单测断言)"。

## 工程化质量 10 维度全覆盖核查（用户指定项，写入 CODE_REVIEW_FULL.md 第十三节）
- 方法：Grep 全仓 + 关键文件精读（request.ts/route-guard.tsx/auth.ts/config/*/logger.ts/package.json/Dialog/schedule）。
- 新 🔴 3 项（全项目 🔴 累计升至 14）：
  - **C-01 越权（🔴）**：route-guard.tsx:116 仅鉴权(`if(profile||isPublicPage)`)无授权层，任意登录用户可 URL 直跳任意页；角色系统仅 UI 切换。
  - **F-01 零测试（🔴）**：package.json 无 test 脚本/devDep 无 jest/vitest，全项目零自动化测试。
  - **G-01 Mock 默认开（🔴）**：config/index.ts:14 `VITE_USE_MOCK ?? 'true'` → 生产未显式传 false 即启用 Mock，与 AGENTS.md「生产禁用 Mock」相反；prod.ts/dev.ts 未纠正。发布 CI 必须传 `VITE_USE_MOCK=false TARO_API_BASE_URL=<真实>`。
- 新 🟡 约 18 项：A-01 数值边界、B-01 缺全局错误边界、C-02 mock 明文密码比对、C-04 BASE_URL 缺省、D-01 timer 清理不一致、D-03 列表无虚拟化、E-01/02 devDep 版本可疑/冗余 babel 插件、G-02/03/04 env 缺省+无 .env.example+源码散落 .bak/脚本、H-01 裸 console 进生产(PII 风险)、H-02 无远程监控、H-03 调试注释、I-01 组件 JSDoc 未验证、I-02 调试注释、J-01/02 跨平台仅 1 处分支。
- 已明示"仍可能遗漏/未全量验证"11 项（A 逐函数边界单测、B 全页 try/catch 率、C 越权资产清单、D 200 处 timer 全审、J h5/tt 实跑、网络重试/abort、i18n、a11y、Mock→真实迁移、Zustand 竞态、隐私合规范围）。
- 结论：前序报告覆盖"业务正确性"，本节补足"工程健壮性/安全/可维护性"10 项。G-01/C-01 属生产直接风险，与 L-08/L-09 同列 P0。

## 前后端联调专项审查建议（用户问"进联调前还有什么审查建议"，写入 CODE_REVIEW_FULL.md 第十五节）
- 决定性核查：`grep -rL VITE_USE_MOCK src/services/*.ts` → 22 个 service 中 **teacher.ts/lead.ts/onboarding.ts/store-entry.ts 4 个无开关、直调 mock**；`teacher.ts:3` 注释"联调时替换为 request 调用" → **teacher/lead 核心模块真实后端调用尚未编写**（最大联调阻塞）。
- 好消息：request.ts 已生产级（token/401/错误信封/10s 超时）；17/22 service 已预留双分支，真实路径走 request 的 get/post/put/del。
- 联调 P0 阻塞三项：① 核心模块开关矩阵补齐（P0-1）；② 接口契约对齐 camelCase↔snake_case + 成功码 0/200（P0-2，无 swagger，仅 docs/todo/05-api-migration.md 计划）；③ 空实现桩 L-08/L-09 二选一（P0-3）。
- P1 联调中验证：时间基准统一(后端为准 L-04)、统一视图硬编码(L-02/L-03)、鉴权无授权(C-01)、错误文案透传、网络超时/取消/并发(小程序≈10)。
- 交付：联调前 CheckList 10 项 + 既有 🔴 在联调期的表现映射表。结论："联调最大坑不是 bug 而是 Mock 即实现——核心模块真实接口没写、命名时间两套"。
