# 2026-08-22 工作日志

## 代码审查标准与流程文档
- 产出：`D:\Coding\yunce\yunceTaro\CODE_REVIEW_STANDARD.md`（v1.0）
- 内容：四道审查关卡（Gate0 提交前自检 / Gate1 husky 门禁 / Gate2 合并前 PR 双签 / Gate3 发布前一致性回归）、RACI 角色分工、维度化清单（规范/可读/安全/性能/错误处理/小程序）、**数据一致性专项**（SSOT、薪资状态机单向、列表-详情-缓存同步、乐观更新回滚、跨 Store 同步、跨服务幂等/补偿、Mock 引用一致性）+ DC-01~08 验证场景库 + 发布前回归检查表。
- 对齐既有 `AGENTS.md` 工程化铁律与 `Agents/review.md`；评论标注沿用 🔴🟡💭 火眼眼风格。
- 下一步（可选，未执行）：可在 `AGENTS.md` 第八节增加指向本标准的索引条目。

## 首轮代码审查（按 CODE_REVIEW_STANDARD 执行）
- 产出：`D:\Coding\yunce\yunceTaro\CODE_REVIEW_REPORT.md`
- 自动化门禁：`npm run typecheck` 全绿（0 error），类型层面干净。
- 结论：2 🔴（均数据一致性）+ 6 🟡 + 3 💭。
- 🔴 发现1：历史/未来月份 `mockGetTeachers(month)` 返回 `genMonthSnapshot`（由静态 `mockTeachers` 重生成，忽略内存 `_teachers`），写后刷新静默丢数据（`src/data/teacher.ts:781-802`，store `:284`）。
- 🔴 发现2：`confirmSalary` 用 `getList()`（忽略月份）而扣款类用 `getList(month)`，跨月视图错位/确认变 no-op（`src/stores/teacher.ts:208` vs `:284`）。
- 🟡 关键：getPendingCount 语义错（非 archived≠待确认，`teacher.ts:380`）；mockExecutePay 实发金额≠calcTotal（`teacher.ts:929` vs `:25`）；双教师模型薪资字段不进统一视图 `buildTeacherView`（`mock-database.ts:662-701`）；裸 `<Input>` 约40处；Store 写操作无 try/catch 回滚；SalaryStatus `teacher_confirmed` 悬空。
- 整改：🔴 合并前必修（统一月份数据源 + 修 getPendingCount）；🟡 排期（双模型重构、ESLint 收口裸 Input）。

## 首轮复审（Re-Audit）
- 产出：`D:\Coding\yunce\yunceTaro\CODE_REVIEW_REAUDIT.md`
- 方法：回读真实代码逐行核对 + 排查 UI 可达性/跨模块同步/状态机文档一致性/死代码。
- 裁定：**首轮无误报**；2 项需校准、补充 6 项遗漏。
  - 🔴1 机制修正：实际扣款走 `salary-adjust → updateTeacher(id,{deductions})`，**`addDeduction/updateDeduction/deleteDeduction` 是死代码**（全仓无页面调用）；且过去月被强制 `archived`、未来月被拦截，故"扣款立即消失"在当前 UI 不可达。根因（非当前月数据源非单一可信源）仍属实，定级 **🔴-latent**。
  - 🔴2 降级 🟡：确认/批量确认/发放/发送/增改教师等写后均 `getList()` 无月份（store :208/214/225/236/266/272/278），仅扣款类带 `salaryMonth`（:284/290/296）——不一致属实，但确认只在当前月可达，即时丢失不可达。
  - 新增遗漏（数据一致性专项本应覆盖）：A) `buildTeacherView` 把 `status` 硬编码 `'active'`（`mock-database.ts:672/692`）→ 离职状态不进统一视图；B) 新增教师 `campusIds:[]`；C) 状态机文档 `pending→confirmed→paid` 与实现 `pending→confirmed→sending→archived`+孤儿 `teacher_confirmed` 漂移；D) 应用内两个矛盾"待确认"计数（teacher-list 用错误 getPendingCount vs salary-payment 用正确 stats.pending）；E) 死代码扣款方法；F) 裸 Input 实为 ≈44 处（非约40）。
- 建议：标准 §4.2 增两条检查项「统一视图字段同步完整性」「状态机文档与实现一致性」。

## 全量逐模块代码审查（用户指令：重审全部、不跳读、画图、分 bug/链路）
- 产出：`D:\Coding\yunce\yunceTaro\CODE_REVIEW_FULL.md`（约 31KB，11 节 + 4 流程图 + 26 行问题表）。
- 方法：逐文件通读 stores(13)/services(22)/types(24)/data(26) + 核心逻辑文件 teacher.ts/mock-database.ts/students.ts/lead.ts；Grep 全仓扫断裂调用/字段冲突/双数据源/状态机漂移/时间基准分裂。已纠正一误判（消课链路实际可用，降级双类型风险）。
- 🔴 7 项（P0 必修）：L-08 调班 `mockTransferStudent` 空实现、L-09 结束班级 `mockEndClass` 空实现、L-02 统一教师视图硬 `status='active'`、L-03 双教师主数据同 ID 分裂、L-04 时间基准分裂(mock 2026-06 vs 真实时钟)、L-01 跨月 `genMonthSnapshot` 用静态 `mockTeachers` 忽略内存写入、B-01 `mockExecutePay` 实发算法≠`calcTotal` 展示。
- 🟡 约 18 项：B-05 线索转化 camelCase 脏数据、L-10 双 CoursePackage、L-05 状态机孤儿、L-06 扣款死链、B-02/B-03/B-04 计数/月份/下标、L-11 会员卡与课包不打通、L-12~L-18 待走查、Q-01 Store 无回滚、Q-02 裸 Input≈44。
- 致命点：调班/结束班级用户以为成功实际零改动；教师统一视图/薪资状态不同步；双类型/双主数据/时间基准三类结构性衔接不一致。P0 优先收口。

## 走查清单专项核验（L-11/L-12/L-13/L-14/L-17/L-18 代码级走查，写入 CODE_REVIEW_FULL.md 第十二节）
- L-11 会员卡↔课包：原"未打通"判定过时——代码有发卡/改卡联动(del L-11-A🔴单向只写不读，课后即失同步；L-11-B🟡 name.includes('会员卡')脆弱匹配；L-11-C🟡 remainingHours 可>totalHours 抹掉 usedHours)。
- L-12 临时调课：链路已实现(写storage→读→visible-schedules消费)；新 bug：覆盖键 `${sid}__${sourceDate}` 过窄致同排课多次调动双订(L-12-A🟡)；存储持久+基础排课内存易失错位(L-12-B🟡)。
- L-13 考勤/课时：主线一致(COURSE_PACKAGES as DB_PACKAGES 同数组)；风险：双扣减实现(mockCreateLessonRecord内联 + 独立 mockDeductPackageHours)漂移(L-13-A🟡)，purchasedHours 被误减(L-13-B🟡)。lesson-form 仅走 create 单扣，暂无双扣。
- L-14 线索锁定：weak→locked 写入正确集中(mockCreateLeadBooking L1295)；🔴 关键断裂：mockUpdateLead 用 Object.assign 无守卫，locked 线索归属可被任意改写(L-14-A)；卡片返回了状态但 UI 不区分(L-14-B)。
- L-17 统计：确认预警为硬编码假数据(MOCK_OPERATION_ALERTS/FINANCE_ALERTS 直返 services:175/219/224/312)L-17-A🔴；趋势半真(getVisibleLessonRecords实时 vs MONTHLY_STATS静态)L-17-B🟡。
- L-18 门店入驻：数据层 mockSubmitStoreEntry 调 campusService.add 立即建校区(del L-18-B🔴 与页面"1-3工作日审核"异步文案语义矛盾)；UI 提交后不刷新校区列表(del L-18-A🟡)。
- 走查后累计：新增/升级 4🔴(L-11/L-14/L-17/L-18)+2🟡(L-12/L-13)，全项目 🔴 累计 11 项。每项均附"优化建议 + 可度量验收标准(含 grep/单测断言)"。

## 工程化质量 10 维度全覆盖核查（用户指定项，写入 CODE_REVIEW_FULL.md 第十三节）
- 方法：Grep 全仓 + 关键文件精读（request.ts/route-guard.tsx/auth.ts/config/*/logger.ts/package.json/Dialog/schedule）。
- 新 🔴 3 项（全项目 🔴 累计升至 14）：
  - **C-01 越权（🔴）**：route-guard.tsx:116 仅鉴权(`if(profile||isPublicPage)`)无授权层，任意登录用户可 URL 直跳任意页；角色系统仅 UI 切换。
  - **F-01 零测试（🔴）**：package.json 无 test 脚本/devDep 无 jest/vitest，全项目零自动化测试。
  - **G-01 Mock 默认开（🔴）**：config/index.ts:14 `VITE_USE_MOCK ?? 'true'` → 生产未显式传 false 即启用 Mock，与 AGENTS.md「生产禁用 Mock」相反；prod.ts/dev.ts 未纠正。发布 CI 必须传 `VITE_USE_MOCK=false TARO_API_BASE_URL=<真实>`。
- 新 🟡 约 18 项：A-01 数值边界、B-01 缺全局错误边界、C-02 mock 明文密码比对、C-04 BASE_URL 缺省、D-01 timer 清理不一致、D-03 列表无虚拟化、E-01/02 devDep 版本可疑/冗余 babel 插件、G-02/03/04 env 缺省+无 .env.example+源码散落 .bak/脚本、H-01 裸 console 进生产(PII 风险)、H-02 无远程监控、H-03 调试注释、I-01 组件 JSDoc 未验证、I-02 调试注释、J-01/02 跨平台仅 1 处分支。
- 已明示"仍可能遗漏/未全量验证"11 项（A 逐函数边界单测、B 全页 try/catch 率、C 越权资产清单、D 200 处 timer 全审、J h5/tt 实跑、网络重试/abort、i18n、a11y、Mock→真实迁移、Zustand 竞态、隐私合规范围）。
- 结论：前序报告覆盖"业务正确性"，本节补足"工程健壮性/安全/可维护性"10 项。G-01/C-01 属生产直接风险，与 L-08/L-09 同列 P0。

## 前后端联调专项审查建议（用户问"进联调前还有什么审查建议"，写入 CODE_REVIEW_FULL.md 第十五节）
- 决定性核查：`grep -rL VITE_USE_MOCK src/services/*.ts` → 22 个 service 中 **teacher.ts/lead.ts/onboarding.ts/store-entry.ts 4 个无开关、直调 mock**；`teacher.ts:3` 注释"联调时替换为 request 调用" → **teacher/lead 核心模块真实后端调用尚未编写**（最大联调阻塞）。
- 好消息：request.ts 已生产级（token/401/错误信封/10s 超时）；17/22 service 已预留双分支，真实路径走 request 的 get/post/put/del。
- 联调 P0 阻塞三项：① 核心模块开关矩阵补齐（P0-1）；② 接口契约对齐 camelCase↔snake_case + 成功码 0/200（P0-2，无 swagger，仅 docs/todo/05-api-migration.md 计划）；③ 空实现桩 L-08/L-09 二选一（P0-3）。
- P1 联调中验证：时间基准统一(后端为准 L-04)、统一视图硬编码(L-02/L-03)、鉴权无授权(C-01)、错误文案透传、网络超时/取消/并发(小程序≈10)。
- 交付：联调前 CheckList 10 项 + 既有 🔴 在联调期的表现映射表。结论："联调最大坑不是 bug 而是 Mock 即实现——核心模块真实接口没写、命名时间两套"。

## Git 仓库损坏恢复 + 推送 Gitee 成功
- 现象：`git status` 报 `bad object HEAD`——本地 master 指向的提交 `1524808` 对象在 objects 里完全丢失（连同 reflog 不可恢复）。
- 恢复流程（已验证）：① `git update-ref refs/heads/master <origin/master sha>`；② 损坏 index 删不掉时 `rm .git/index && git read-tree HEAD` 重建；③ 工作区改动 `git add -A` 重建提交 `a88d5cd`（97 文件，含 docs 重组、campus-card-redesign 清理、src 修复）；④ 推送成功 `76b1744..a88d5cd`。
- **推送认证修复**：GCM 在非交互 shell 里静默返回空凭据 → 卡死/回退 tty 提示。本仓库 `.git/config` 已改为 `credential.helper=""`（清空链）+ `credential.helper=wincred`，直接读 Windows 凭据管理器 `git:https://gitee.com`（账户 **15890006269**，用户确认）。推送命令：`GIT_TERMINAL_PROMPT=0 git push origin master`。
- **沙箱怪癖（重要）**：git ref 事务（lockfile rename）会触发沙箱安全删除，把 `.git/refs/remotes/origin/` 整目录隔离——push/fetch/update-ref 成功后本地跟踪引用反而消失（status 显示 `[gone]`）。修复：直接写文件 `mkdir -p .git/refs/remotes/origin && printf '<sha>\n' > .git/refs/remotes/origin/master`（绕过 git 事务）。**以后每次推送后若见 `[gone]` 照此重写即可，推送本身是成功的。**
- 澄清：commit 作者邮箱 `151536422@qq.com` 与 Gitee 认证账户 `15890006269` 是两回事，无需修改。

## CODE_REVIEW_FULL.md 修复批处理（17:00-18:15）

按 TaskList #1-#12 顺序逐项修复 14 个 🔴 中的 11 个，每项编译+typecheck+commit+push：

- **B-01 (#10, 22298be)**：`calcTotal` 从 stores/teacher.ts 移入 data/teacher.ts，`mockExecutePay` 复用同一算法，消除实发≠展示。
- **L-18 (#11, 4e8b5fa)**：门店入驻提交后主动 `fetchCampuses()`+`setAllowedCampusIds` 刷新校区可见性；status 区分 approved(同步建校区)/pending(申请单)，文案随之统一。
- **F-01 (#12, c8f9c92+33b52d7)**：引入 vitest 2.1.9 + @vitest/coverage-v8 2.1.9，9 条回归单测（calcTotal 4/lead 锁定守卫 3/store-entry 2）。
  - vitest 安装用 `--legacy-peer-deps --cache /tmp/npm-cache-vitest` 绕开 EPERM；`node_modules/.bin` 无软链，package.json scripts 改 `node node_modules/vitest/vitest.mjs run`。
  - teacher.test.ts 需 `vi.mock('@/data/mock-database')` 阻止 buildTeacherView 顶层循环依赖初始化。
  - `vitest.config.ts` 设 `cache: false` 规避 Windows 沙箱 results.json EPERM。
- **REPAIR_PLAN.md 更新**：进度总览 12/15 完成，剩余 C-01(授权层)/G-01(Mock默认值)/P0-1(真实接口,需后端)。

### 剩余 3 项未修
- C-01：route-guard 仅鉴权无授权层，需建 requireRole/withPermission HOC。
- G-01：config/index.ts:14 `VITE_USE_MOCK ?? 'true'` 改 `'false'` + .env.example。
- P0-1：teacher/lead/onboarding/store-entry 补 VITE_USE_MOCK 双分支，**需后端 OpenAPI 契约**。

## REPAIR_PLAN 验收复核（18:24）

逐项对照 commit + 实际代码复核文档宣称的 12 项"已完成"，结论：
- **11 项确认真实完成**（commit 存在 + 代码匹配验收）：L-08/L-09/L-02/L-01/L-03/L-11/L-14/L-04/B-01/L-18/F-01。F-01 已实跑 `npm run test` = 9 passed（3 文件）。
- **L-17 被高估为完成，实际仅部分完成**：运营预警 `mockGetOperationAlerts` 已实时计算、且 `MOCK_OPERATION_ALERTS` 常量已彻底删除；但**财务预警 `mockGetFinanceAlerts` 在无到期课包时仍 fallback 返回静态 `MOCK_FINANCE_ALERTS`（statistics.ts:115 定义、:287 直返）**，service 层 `getFinanceAlertsFallback` 又委托给它 → mock 模式下财务预警仍是假数据。
  - 文档验收标准 `grep MOCK_*_ALERTS src/services/` 只查了 services 层（通过），漏查 data 层，故误判。
  - 建议：把 :287 的静态 fallback 改为与运营一致的"稳定态"实时生成，并在文档把 L-17 标为 ⚠️ 部分完成 / 剩余 finance 路径。
- **3 项 pending 复核确属未完成**：C-01（route-guard 仅 identities.length 判断，无 requireRole/withPermission 越权拦截）、G-01（config/index.ts:14 仍 `?? 'true'`）、P0-1（lead/onboarding/store-entry/teacher 共 4 个 service 仍无 VITE_USE_MOCK 双分支）。

## 完成剩余任务 + 自查 + 业务口径审计（18:45-19:00）

按用户要求完成 CODE_REVIEW_FULL.md 全部剩余项并自查：
- **L-17 补完**：mockGetFinanceAlerts 无到期课包时改实时"稳定态"文案，删除 MOCK_FINANCE_ALERTS 常量及其 AlertItem 接口/import。
- **C-01 授权层**：route-guard.tsx 新增 `PAGE_ROLE_REQUIREMENTS`（页面→角色矩阵，STAFF/MANAGER 两组）、`getProfileRoles`/`requireRole`/`withPermission` HOC；checkAuth 在登录后做越权校验，越权 redirectToForbidden 回首页。⚠️ **角色矩阵是智能体按常识推测的权限，已列入口径审计 A1，待用户确认**。
- **G-01**：config/index.ts `?? 'false'` + 生产守卫（误带 true 强制关+告警）；新建 `.env.example`。
- **P0-1**：lead.ts 用脚本激活 28 处已起草的真实 return + 改 env 模式；teacher.ts 全部 service 补 USE_MOCK 双分支（SendResult/ApplyTemplateResult 泛型对齐）；onboarding/store-entry 双分支。验收 `grep -L VITE_USE_MOCK src/services/*.ts` 仅剩 index.ts。⚠️ endpoint 均为猜测占位，待后端契约。
- **🟡 批**：B-03（写后重拉带 salaryMonth）、B-04（modelIdx 冗余 findIndex 收敛）、Q-01（teacher store 核心写操作 try/catch+错误态）、L-12（调课覆盖键改 schedule_id 防双订）、L-13（消课不动 purchasedHours）、A-01（calcTotal NaN 防御+单测）、B-01(工程)（app.tsx 包 ErrorBoundary + Taro.onError）、C-02（明文密码守卫注释）、G-04（.bak/脚本移 _archive/g04-stray）、L-05（teacher_confirmed 文档对齐）。
- **走查澄清**：B-05 是审查误报（mock-database DB Student 即 camelCase，mapMockStudent 正确桥接，无需改）；L-16 占位入口为良性 toast；L-06/L-07 的 deductHours 仍被办卡预填使用，无实际双扣。
- **自查结果**：typecheck ✅；vitest 10/10 ✅（新增 NaN 防御用例）；`build:weapp`（VITE_USE_MOCK=true）编译成功 36s ✅；lint ⚠️ 全仓 9.5k 个 **CRLF(\r) 预存错误**（prettier 行尾，非本次引入），本次改动文件已 --fix 干净（仅剩 9 个预存 react-hooks useMonthStore-in-data-layer 错误 + import/order warning）。
- **业务口径审计（关键交付）**：已写入 REPAIR_PLAN.md §九，共 15 条（A1-A15），其中 A1 权限矩阵、A6 预警阈值 12、A11 endpoint 占位为最优先确认项；A2 结束班级级联移除学员、A4 会员卡派生/充值语义、A5 forceReassign、A8 入驻即建校区、A10 purchasedHours 等为修复时推断的链路语义。**用户明确要求所有业务口径逐一确认。**
- 产物：dist 已重建（VITE_USE_MOCK=true mock 模式），git 未提交（用户未要求推送，待确认口径后一并提交）。

## 排除后端路径问题 + 业务口径问答（19:50）

- **排除 A11 路径问题（用户要求直接做）**：新增 `src/utils/not-wired.ts`（`notWired(apiName)` 抛"接口未接通待 OpenAPI 契约"显式错误）；把 P0-1 四个 service（teacher/lead/onboarding/store-entry）里**我/前序会话猜测的 REST endpoint 全部替换为 notWired()**（lead 27+1、teacher 29、onboarding 1、store-entry 1，共 58 处），VITE_USE_MOCK=false 时不再请求猜测 URL、直接报错。其余 14 个既有双分支 service（auth/campus/student/statistics/temporary-reschedule 等）是**项目原有契约草案**（审查 P0-2 已登记联调核对），未动。
- 验证：typecheck ✅、vitest 10/10 ✅、build:weapp 编译成功 29s ✅。
- 已用 AskUserQuestion 向用户询问关键业务口径（权限矩阵/结束班级/预警阈值/会员卡语义），其余项以文字建议列表待用户回复。REPAIR_PLAN.md §九 A11 行已更新为 ✅ 已排除。

## 用户口径确认并落地（20:00）

用户回答 4 个关键口径问题，全部落实：
- **权限体系（A1，重要）**：用户完整描述角色模型 → 新增 `src/types/permission.ts`（DataScope: own/campus/all；DataModule 7 类；ROLE_PERMISSION_MAP；resolveDataScope/canAccessModule/hasBoundChild）；route-guard `PAGE_ROLE_REQUIREMENTS` 按用户口径重排（admin/principal 全量；teacher=own 可进薪资详情/考勤/学员/班级/线索；assistant 无薪资；parent 禁入所有管理页；经营看板仅 admin/principal）。**授权开关 UI 未实现，规划文档 `docs/permission-model.md`（P0 基座✅/P1 授权 UI⏳/P2 接口级校验⏳/P3 审计⏳）**。
- **结束班级（A2）**：用户澄清"班课固定循环上课不结束；排课结束也不移除学员" → `mockEndClass` 已去掉级联移除学员，仅标记 ended。
- **预警阈值（A6）**：用户选"做成可配置" → 提取 `OPERATION_ALERT_THRESHOLD_HOURS = 12` 常量（statistics.ts）。
- **会员卡（A4/Q4）**：用户确认"点名签到按次数扣课时；无单独增次只能充值会员卡（可设免费赠送卡）" → 现有派生+充值语义符合，赠送走 bonusHours（结构已支持），无需改码。
- 待确认（默认按建议保留）：A3 调班单向迁移、A5 forceReassign、A7 文案、A8 入驻即建校区、A9 最新调课、A10 purchasedHours、A12 计数、A13 modelIdx、A14 双类型、A15 占位。
- 验证：typecheck ✅、vitest 10/10 ✅、lint 改动文件 0 error ✅、build:weapp 24s ✅。git 仍未提交。

## 用户 6 点回复落地（20:10-20:22）

- **① 权限体系大工程（已实现）**：
  - admin 可编辑 **principal/teacher/assistant** 模块开关 + 数据范围；admin 自身全量只读
  - 新增 `types/permission.ts` 扩展（CustomRole/RoleGrant/PermissionConfig/defaultRoleGrant）
  - 新增 `services/permission.ts`（storage 持久化 + USE_MOCK 双分支）、`stores/permission.ts`（zustand CRUD）
  - 新增授权页 `package-settings/pages/permission-settings`（系统角色开关 + 自定义角色新建/编辑/删除/保存），app.config.ts 注册路由，system-settings 加"角色权限"入口
  - route-guard 新增 `PAGE_MODULE_MAP`（页面→模块）+ `hasModuleAccess`（读持久化 grants，未覆盖回退默认）——角色门槛后按模块授权动态放行
  - **深度建议文档 `docs/permission-recommendation.md`**（9 岗位/模块矩阵/6 场景推理/默认模板/安全边界）
- **② 预警阈值配置化（已实现）**：`utils/alert-config.ts`（get/set + 默认12）+ 系统设置页"课时不足预警阈值"输入入口（showInputModal editable）+ statistics 实时读取
- **③ 接口路径**：真实接口接入放最后（notWired 占位保持），联调时再做 ✅
- **④ 结束班级语义（已澄清+固化）**：`mockEndClass` **无任何页面调用**（概念来自审查发现的空桩）；真正解散走 `mockDeleteClass`（**已正确移除学员分班**）；下课/排课结束不移除。mockEndClass 保留为预留接口+注释
- **⑤⑥ 其余口径**：按推断保留，已给用户测试指引（权限页/预警配置/解散班级均可实测）
- 技术细节：Taro showModal 类型缺 editable → 新增 `utils/modal.ts` showInputModal 补齐
- 验证：typecheck ✅、vitest 10/10 ✅、lint 0 error ✅、build:weapp 63s ✅。git 未提交

## 用户实测反馈重做（20:28-20:35）

- **问题1：预警阈值弹框** → 抛弃弹框，改为**专用表单页** `package-settings/pages/threshold-config/index`（FormRow editable inputType="number" suffix="课时"，Card 分组，参考 course-form 设计），默认值改为 **5**（用户测试口径），系统设置行点击 → navigateTo 该页
- **问题2：权限 UI 重做**（用户给出明确设计）：
  - **list 页** `permission-settings`：干净的列表 = 系统角色卡片（管理员只读/校长老师前台可点击进入编辑）+ 自定义角色卡片（点击进入编辑/带删除按钮）+ 底部"新建角色"按钮
  - **form 页** `package-settings/pages/permission-form/index`：单个干净的全屏表单，mode=create/edit + key 参数；字段：角色名称（系统角色 disabled / 自定义可编辑）+ 数据范围 3 chip + 7 模块开关（FormRow + 行内 toggle），底部"保存"→ 持久化 → 返回 list
  - 表单样式严格参考 `package-course/pages/course-form`（Card + FormRow + PageContainer + ScrollView + UnoCSS token）
- route 注册 `permission-form` / `threshold-config` 两个新页
- 清理：删除 system-settings 中的 showInputModal 弹框路径（改为 navigateTo）；threshold-config 不依赖 theme store
- 验证：typecheck ✅、lint 0 error ✅、build:weapp 57s ✅、dist 含三个目标页（permission-form/permission-settings/threshold-config）✅
- 仍未 commit，等用户实测确认

## 用户实测发现：UI 重复组件未共享 + 失效图标（21:05）

- **问题1（UI 不一致根因）**：`course-form/index.tsx` 的"上课学员"区块**没有引用** `components/course/ClassStudentsCard`，而是自己 inline 写了 ~104 行重复代码（包含同样的网格/删除按钮/添加按钮）。`schedule-form` 使用了 `ClassStudentsCard`，所以两边漂移是必然的。修复：删除 inline，改为引用共享组件 `<ClassStudentsCard>`；清理孤儿代码：移除 `studentSelectVisible` state、useEffect 中的 dep、顶部外部 `<StudentMultiSelectSheet>` 渲染、`Avatar`/`StudentMultiSelectSheet` import、`studentLoading` state（已无消费者）。两个页面现在都走 ClassStudentsCard。
- **问题2（失效图标）**：`help/index.tsx:392` 使用 `mdi-phone-outline`，但图标库只有 `mdi-phone`（无 outline 变体），console 报 `[Icon] unknown icon name: mdi-phone-outline`。改为 `mdi-phone`。
- 验证：typecheck ✅、lint 0 error ✅（2 个无关预存 hooks warning）、build 56s ✅。

## 用户实测：万老师数据一致性（21:25）

**现象**：以万老师（user-principal-001，admin）登录 → ① 课表编辑页选老师没有"万老师"；② 首页今日课表为空。

**根因排查**：
1. **首页为空**：`mockGetTeacher`（data/home.ts）找不到万老师的教师档案（TEACHERS 无 userId 对应记录），fallback 分支 `if (scope.role !== 'principal') return null` **不认 admin** → 返回 null → home `loadData` 提前 return，今日课表永不加载。
2. **教师选择器缺人**：万老师不在教师主数据（`_teachers`/rawMockTeachers 与 BASE_TEACHERS 均无记录），`mockGetTeachers/getActiveList` 自然列不出她。

**修复（3 处）**：
1. `BASE_TEACHERS`（mock-database.ts）新增 `teacher-principal-001`（userId=user-principal-001，3 校区，accessScope org）
2. `rawMockTeachers`（data/teacher.ts）新增同 id 教师档案（跨 3 校区，status active）→ 选择器/薪资列表可见
3. `mockGetTeacher` fallback 放宽为 `role !== 'principal' && role !== 'admin'` 才 return null → admin/principal 无档案时给聚合摘要

**验证链**：mockGetTeacher 经 buildTeacherView 找到新档案（id=teacher-principal-001）→ getTodaySchedules → filterSchedulesByActor 走 org 范围 → 今日（周六）3 节全可见。typecheck ✅ 单测 10/10 ✅ build ✅。
- 注意：本次未触碰 L-03（BASE teacher-001 张老师 vs 管理库 teacher-001 王校长 userId 冲突）这个历史问题，属既有数据债。

## L-03 教师数据链彻底修复（21:36，用户要求"把数据链搞清楚"）

**梳理结论（四表 + 引用）**：
- 权威体系 = USERS + IDENTITIES + BASE_TEACHERS（张/李/王/赵/万），teacher-00x id 全仓 80+66+35+44 处引用（班级/排课/学员）语义本就指向这 4 人
- 唯一错乱源 = 管理库 `rawMockTeachers` 名字（teacher-001 王校长 / teacher-003 张助教 / teacher-004 赵前台），导致 buildTeacherView 把 BASE 的 userId 补错位：teacher1(张老师) 登录命中"王校长"档案、teacher3 命中"张助教"、teacher4 命中"赵前台"，仅 teacher2(李老师) 正常

**修复**：
1. `rawMockTeachers`（data/teacher.ts）三条对齐：teacher-001→张老师(钢琴/13800000011/center/identity teacher)、teacher-003→王老师(舞蹈/13800000013/east/role lead 主讲)、teacher-004→赵老师(书法/13800000014/west/role lead)；teacher-002 phone 对齐 13800000012
2. 排课校区核对：001→center、003→east、004→west 与 BASE 一致
3. 孤儿引用清理：cls-004/cls-005 的 `teacher-012`（不存在）移除 → `['teacher-002']`
4. **新增回归测试 `src/data/teacher-chain.test.ts`**：锁 5 账号 userId→teacherId→name 一致性 + 无"王校长/赵前台"残留 + mockGetTeacher 链路命中正确档案
5. 已知残留（未处理，显示层无碍）：lead.ts 试听可预约时段种子里 teacher-006/007/009（周/吴/刘老师）为虚构教师，自带 teacherName 仅展示用；teacher-005 陈老师(离职) 无账号属合法

## 会员卡包/消课记录数据链路修复（21:42，用户实测"胡写对不上"）

**审计（自动化对账，member-card-chain.audit.test.ts A1~A5）**：
- A1 课包内部不变量 ✅；A2 学员级对账 ❌ **68/76 学员对不上**（种子消课记录随机生成，与课包 usedHours 脱钩）；A3 引用 ✅；A4 种子课包无 memberCardId（提示级）；A5 撤销/删除消课**空实现/不回补** ❌

**修复**：
1. `generateLessonRecords` 重写：按学员分配"可消预算=Σ课包 usedHours−固定示例已消"，签到逐步消耗；课包用尽后 0 课时 → A2 归 0
2. 补 `pkg-012/013/014`（stu-063/064/068 国画课包 used2/rem22）消除"无课包有消课"
3. `mockRevokeLessonRecord` 空桩→实现回补；`mockDeleteLessonRecord` 补回补（`refundPackageHours` 按 packageId 回补 remaining/回扣 used，finished→active）
4. `mockCreateLessonRecord` 统一落驼峰 `hours`/`packageId`（此前仅 snake hours_used/package_id，回补读不到）；LessonRecord 类型加 `packageId?`
5. 全仓测试 **18/18**；审计报告 `docs/lesson-record-chain-audit.md`

## P1 欠课机制 + P4 编辑课时 + P5 示例日期（21:56 用户口径落地）

- **P1 欠课机制（完整闭环）**：
  - 新增 `types/lesson-debt.ts`（LessonDebt：pending/settled + deduct划扣/waive平账）、`data/lesson-debt.ts`（storage 持久化：getPendingDebtsByStudent/addDebt/settleDebts 支持部分抵扣）、`services/lesson-debt.ts`
  - `class-checkin`（签到页）：课包**按班级科目检索**优先；科目课包不足 → showActionSheet 三选「欠课上课/划扣其他课包/取消」；欠课上课 → 记录不扣课包(package_id='')+记欠课台账；划扣 → 再列可划扣课包选一个扣
  - `member-card-issue`（开卡页）：发卡后查欠课 → showActionSheet「划扣抵扣/平账豁免」；划扣 → `memberCardService.deductDebt`（新卡关联课包剩余抵欠课，返回未抵完）+ settleByStudent(deduct, 额度)；平账 → settleByStudent(waive)。新增 `deductCardDebtHours`（data/member-card）
- **P4 编辑课时（已实现，需用户人工审查设计）**：`lesson-detail` 页撤销按钮上方加「编辑课时」按钮 → BottomSheet 输入新课时 → `lessonRecordService.update`（新增 mockUpdateLessonRecord：差额自动回补/追扣关联课包）
- **P5**：补课示例记录日期改为动态"最近的非周日"
- **A2 审计公式更新**：允许 recHours >= pkgUsed（欠课记录计入记录但不计课包消耗）
- 验证：typecheck ✅、18/18 ✅、lint 改动文件 0 error ✅、build ✅

**页面走查建议（未做，待用户定）**：P1 选课包策略只取第一个剩余足够的（多课包可能误判课时不足）→ 建议最早到期/剩余最多优先；P2 记录 snake/camel 双轨（仅 hours/packageId 归一）；P3 种子课包 memberCardId；P4 无 updateLessonRecord（改记录需同步课时）；P5 示例记录固定日期 6/28 会过时。验证：typecheck ✅、18/18 ✅、build 34s ✅。git 未提交。

**验证**：typecheck ✅、单测 13/13 ✅（含新 3 条）、build ✅。

## 操作日志（审计日志）+ 编辑课时权限收紧（22:30）
- **用户口径**：编辑课时=高权限操作仅管理员（admin+principal）可用；日志不可修改（append-only）；系统设置→操作日志页面；关键操作持久化；保留 90 天。
- **新增**：`types/audit-log.ts`（AuditAction 10 类 + AUDIT_ACTION_LABELS）、`data/audit-log.ts`（storage 持久化 `yunce-edu-audit-log`，add/query 无 update/delete，90 天 TTL 惰性清理，Node 测试自动内存 fallback）、`services/audit-log.ts`（双分支 notWired）、`package-settings/pages/audit-log/index.tsx`（时间tab/动作chips/关键字筛选/分页加载）、`src/data/audit-log.test.ts`（5 用例：追加/过滤/分页/日期/90天清理）。
- **入口**：system-settings 原「操作记录」占位 → 改为「操作日志」（managerOnly：admin+principal 可见），app.config 注册 audit-log 路由。
- **权限收紧**：lesson-detail「编辑课时」按钮从 isTeacher 块拆出 → 仅 isManager（admin/principal）可见；撤销消课仍 isTeacher。
- **埋点 10 类**：编辑课时/撤销消课（lesson-detail）、开卡（member-card-issue）、充值调整（member-card-edit）、薪资核对/发送（salary-payment）、薪资发放（useTeacherList）、权限保存（permission-form）、预警阈值（threshold-config）、解散班级（schedule batch-delete）。
- **踩坑**：Profile 无 username 字段（用 name）；permission-form 的 handleSave 非 async（埋点改 void+catch）；data 层 addAuditLog 自动填 actionLabel；lesson-detail 埋点引用 record 需可选链+deps 补 record。
- 验证：typecheck ✅、tests 23/23 ✅（新增 5 条）、lint 改动文件 0 error ✅、build:weapp 进行中。git 仍未提交。

## 操作日志可见范围 + 分类清单（22:50）
- **用户口径**：每个人只看自己的日志；管理者（admin/principal）可看下属全部日志；只记重要信息不记垃圾操作；出分类清单审核。
- **改造**：
  - `types/audit-log.ts` AuditLogQuery 加 `operatorId` 精确过滤
  - `services/audit-log.ts`：query 签名改为 `query(viewer, query?)`，**非管理角色强制 operatorId=viewer.id 防越权**；新增 `isAuditLogManager(role)`（admin/principal）
  - 页面 `audit-log/index.tsx`：入口对所有登录角色开放；管理角色有「全部员工/仅我的」切换 tab；普通角色固定只看自己（顶部提示）；service 层双重保险
  - `system-settings`：操作日志入口去掉 managerOnly（所有角色可见）
- **测试**：新增 `services/audit-log.test.ts`（4 用例：管理看全部/定向查某员工/非管理被强制只看自己/角色判定），data 层加 operatorId 过滤用例 → 全仓 **28/28**
- **分类清单**：`docs/audit-log-catalog.md` —— A 资金财务 5 类 / B 高权限数据变更 3 类 / C 系统配置 2 类（阈值待确认）；明确不记录清单（签到消课/调课/只读/登录等）；3 个待拍板问题（阈值去留/欠课是否同步记/自定义管理岗是否按校区看下属）

## 操作日志修正：签到入日志 + 长期保留仅展示90天（23:00）
- **用户口径修正**：① 点名签到是重要日志（批量汇总记 1 条，学员级明细在消课记录表）；② 后端长期保留（落库不删），前端仅展示最近 90 天——我之前的"90 天 TTL 删除"是错的。
- **改动**：
  - `types/audit-log.ts` 新增 `lesson.checkin`（点名签到）action+label
  - `data/audit-log.ts`：**删除 prune TTL 清理**（数据永不删）；`AUDIT_LOG_RETENTION_DAYS` 改名 `AUDIT_LOG_DISPLAY_DAYS=90`；query 强制过滤"仅返回最近 90 天"（展示上限，数据保留）
  - `class-checkin/index.tsx`：批量签到成功后记 1 条汇总日志（班级/人数/课时），operatorName 用 profile.name；deps 补 profile/setLoading
  - 页面：时间 tab 改「近7天/近30天/近90天」，顶部/底部文案改为"展示最近 90 天（历史长期保留）"
- **测试**：90 天 TTL 用例改为"展示范围过滤（数据不删）"用例；28/28 通过
- 文档 `docs/audit-log-catalog.md` 同步更新（签到入 B 类、保留策略第八节修正）

## 操作日志补充：单人消课/补课 + 创建班级（23:05）
- **用户口径**：单人消课也要记日志；创建班级、解散班级都记录。
- **改动**：
  - `types/audit-log.ts` 新增 `lesson.record`（单人消课/补课）、`class.create`（创建班级）→ 共 13 类
  - `lesson-form` handleSingleSubmit 成功 → lesson.record（学员/课包/课时/跨科目）
  - `lesson-supplement` 补课登记循环后汇总 1 条 lesson.record（学员名单/课时）
  - `class-form` create 分支成功 → class.create（班级名/学员数）；**编辑班级不记**
  - 文档分类清单更新：B 类 4→6（加点名签到/单人消课/补课/创建班级）
- 验证：typecheck ✅、tests 28/28 ✅、lint 0 error ✅、build 中。git 未提交。

## 操作日志：线索链路 + 全面检索补漏（23:20）
- **用户口径**：线索功能也要埋点（谁邀请/谁跟进/谁转化=重要运营数据）；其他场景全面检索补漏。
- **新增 7 类埋点**（共 20 类）：
  - 线索：`lead.create`（新建线索，lead-form/班课预约）、`lead.follow`（跟进，lead-detail）、`lead.convert`（转化，lead-detail）
  - 补漏：`student.transfer`（学员调班，student-transfer）、`staff.add`（新增教师，teacher-form create 分支）、`staff.resign`（教师离职，teacher-list，ResignType=quit/dismiss/expire）、`store.apply`（门店入驻申请，store-entry）
- **刻意不记**：调课、只读、登录退出、欠课/划扣（台账已有）、编辑班级/学员/课程。
- **踩坑**：ResignType 是 'quit'|'expire'|'dismiss'（不是 voluntary/dismissed）；多处页面需补 useAuth profile；lead-form 需补 deps。
- 验证：typecheck ✅、tests 28/28 ✅、lint 0 error ✅、build 中。git 未提交。

## 预警阈值最终口径：默认5 + 剩余0强制提醒（23:40）
- **用户口径**：预警阈值可调整、默认 5（剩余 ≤5 提醒）；**剩余 0 课时（最后一节课上完）强制提醒一次，不受阈值影响**。
- **改动**：
  - `data/statistics.ts` mockGetOperationAlerts：过滤条件改为 `remainingHours <= threshold || remainingHours <= 0`（双规则+注释）；剩余 0 学员 info 文案"课时已用尽，请尽快续费"，标题在含已用尽时变"课时不足/已用尽预警"
  - `utils/alert-config.ts`：加内存态 `_threshold`（storage 不可用时读写一致，可测试）
  - `threshold-config` 页：说明文案加"剩余课时用尽（0 课时）时始终提醒"
  - 新增 `src/data/statistics.test.ts`（4 用例：默认5/常规≤阈值/阈值0时剩余0强制/大阈值覆盖）
- 验证：typecheck ✅、tests **32/32** ✅、lint 0 error ✅、build 中。git 未提交。

## 预警触发时机 + 去重（23:56）＋ git 已提交推送
- **git**：今日全部改动已 commit `1a7e44d` 并推送 Gitee master（33b52d7..1a7e44d），跟踪引用已修复。
- **用户口径（预警时机）**：下课后扣完课时、剩余降到阈值 → **立即提醒一次**；**同一轮不重复推送**；剩余回升（撤销回补/充值加课时）后再次下降可重新提醒。
- **实现**：
  - `data/operation-alert.ts`：去重记录（storage `yunce-op-alert-sent`），`checkThresholdAlert` 返回 triggered/duplicate/none；`resetStudentAlert` 剩余回升时清除；剩余 0 强制（阈值 0 也触发）
  - `services/operation-alert.ts`：`countTriggeredAlerts`（批量汇总）+ `clearStudentAlert`
  - 接入 6 点位：class-checkin 批量签到、lesson-form 单人消课、lesson-supplement 补课（触发+toast 并入汇总文案"N名课时不足已提醒"）；lesson-detail 编辑课时（追扣触发/回补清除）、撤销消课（清除）；member-card-edit 充值（清除）
- **测试**：新增 `operation-alert.test.ts`（5 用例：首次触发/不重复/超阈值none/剩余0强制/reset后可重触发）→ 全仓 **37/37** ✅；lint 0 error ✅；build 中。

## 预警提醒改版：首页待办 + 手动已读（08-23 00:20）
- **用户纠正**：预警提醒不是 toast，而是**首页待办事项**中给出，**用户手动点已读**。
- **实现**：
  - 新增 `utils/todo-read.ts`：待办已读记录（`yunce-todo-read`），`markTodoRead/isTodoRead/clearTodoRead/rechargeAlertTodoId`（学员续费提醒待办 id = `alert-recharge-{sid}`）
  - `services/home.ts` getTodoItems：**运营预警拆学员级待办**（每条 detail 一条「XX 课时续费提醒」），已读过滤；finance 保持预警级待办也过滤已读；新增 `markTodoRead` service
  - `TodoList` 组件：每项右侧加「已读」按钮（onMarkRead 回调，stopPropagation）
  - `home/index.tsx`：handleMarkTodoRead → service 记录 + 本地移除
  - 移除 4 处 toast（class-checkin/lesson-form/lesson-supplement/lesson-detail）；充值/撤销/编辑课时回补 → `clearTodoRead(rechargeAlertTodoId(sid))`（可重新提醒）
  - **删除** operation-alert.ts / operation-alert.test.ts / services/operation-alert.ts（被 todo-read 取代）
- **测试**：新增 `utils/todo-read.test.ts`（4 用例）→ 全仓 **36/36**；lint 0 error；build 中

## 首页 tab 卡片样式修复（08-23 00:30）
- **问题**：今日课表内容页有"额外的白色卡片"；待办事项内容页空态"没有白色卡片"。
- **定位**：`TodayScheduleCard` 外层容器 `<View className="... bg-card">`（无圆角无阴影白底）套着内层每课 bg-card 圆角卡片 → 白底套白卡片，外层多余。
- **修复**：
  - `TodayScheduleCard`：外层容器去掉 `bg-card`（空态+内容态两处）→ 只剩内层每课卡片
  - `TodoList` 空态：改为白色卡片容器（bg-card rounded shadow，文字居中）→ 待办空态也有卡片
- 验证：typecheck ✅、lint 0 ✅、build 中；将随本次改动 commit+push 备份。

## 首页课表卡片：左空隔 + 班级颜色（08-23 00:40）
- **问题**：① 课表卡片左边有空隔（比待办卡片多 28rpx）；② 卡片左侧时间区固定橙色，未用班级颜色。
- **修复**：
  - `TodayScheduleCard`：外层容器去掉 `px-[28rpx]`（title 单独保留 padding）→ 卡片左边距与待办 tab 对齐（28+24，不再多 28）
  - 左侧时间区：优先用 `item.color`（ScheduleColor）映射 `scheduleColors`（theme.ts，bg/text 内联 style）；urgent/done/ended 状态色优先；无 color 回退橙色
  - mock：`mock-database.ts` SCHEDULES 16 条补 `color`（按班级就近映射：cls-001 primary / cls-002 info / cls-003 lavender / cls-004 accent...）；mock 内部 Schedule 接口加 color 字段
  - `services/home.ts` mapTodaySchedule 透传 `color` 到 UI Schedule
- 验证：typecheck ✅、tests 36/36 ✅、lint 0 ✅、build 中。

## 课程管理链路打通 + 编译 ENOENT（08-23 00:55）
- **问题**：① 点课程管理报 ENOENT（dist/package-course/sub-common/765d...js 缺失）——产物无此引用，属**微信开发者工具编译缓存**引用旧 chunk hash，需清工具缓存；② 排课的课程不在课程管理班课显示——课程管理展示"课程模板"（course-template），排课针对"班级实例"（CLASSES），两套数据脱节。
- **修复（链路）**：`course-management` 班课 tab 增加「排课中的班课」区块：`classService.getByTeacher` 拉活跃班级（admin 可见全部校区班级），展示名称/排课中标签/学员数/课时进度，点击进 class-detail；countText 计入班级数。
- **ENOENT**：产物已重建（重命名 dist 全新编译）；剩余靠用户清微信开发者工具缓存（详情/工具→清缓存→全部清除）解决。
- 验证：typecheck ✅、tests 36/36 ✅、lint 0 ✅、build 中。

## 课程管理卡片按用户口径精简（08-23 01:05）
- **用户口径**：① 移除"X 个班课模板 · Y 个排课中的班课"和"排课中的班课（N）"两条统计字样；② 班级卡片简约：圆柱高一点、名称小字；③ 排课中标签太多噪音移除，**仅未排课班级加"未排课"标签**；④ 未排课班级点击→class-form（编辑班级），已排课→class-detail，模板→course-form。
- **改动**：
  - `mock-database.ts` 新增 `getScheduledClassIdSet()`（已排课班级 id 集合）
  - `services/student.ts` 新增 `classService.getScheduledClassIds()`（双分支 mock/真后端 notWired）
  - `course-management/index.tsx`：移除 countText 综合统计+区块标题；卡片圆柱 h-[88rpx]（加高）、名称 text-[26rpx]（小字）、排课中标签去掉、改"未排课"标签；点击按 hasSchedule 分流 class-detail / class-form
- 验证：typecheck ✅、tests 36/36 ✅、lint 0 ✅、build 中。

## 课程管理：移除包装器（08-23 01:10）
- **用户口径**：班级卡片外面的包装器（<View mb + 内层 flex gap）应移除，卡片直接呈现。
- **修复**：删除 countText useMemo + 顶部统计渲染行；删除班级卡片的 `<View mb-[24rpx]>` 外层和内层 `<View gap-[16rpx]>`，让班级 map 与模板 map **合并到同一个 `<View gap-[20rpx]>` flex 容器**——卡片之间统一 gap-[20rpx]，无任何外层包装。
- 验证：typecheck ✅、tests 36/36 ✅、lint 0 ✅、build 中。

## 课程管理：模板识别未排课 + 标签主题色（08-23 01:15）
- **用户口径**：① 美术/书法等**模板也是"未排课"**（无排课关联的课程母版要提醒）；② "未排课"标签**用主题色**（bg-primary-bg + text-primary，同旧"排课中"标签样式），不要灰色。
- **改动**：`course-management` 班课 tab 下**模板卡片也显示"未排课"主题色标签**（模板默认视为未排课，点击仍进 course-form 编辑）；班级未排课标签从 bg-muted 灰改主题色。
- 验证：typecheck ✅、tests 36/36 ✅、lint 0 ✅、build 中。

## 移除班级类型概念（08-23 01:25）
- **用户口径**：抛弃"循环上课/课时制"班级类型，班级就是班级；课时数/是否循环统一放排课功能；class-detail 查看逻辑不变。
- **移除**：
  - `class-form`：上课类型 SegmentedControl、总课时数、日期范围、相关校验/state/提交传参（type 固定 'unlimited'）
  - `CreateClassSheet`：上课类型 SheetTag、关联课程包选择器（含弹窗）、日期范围、相关 props
  - `useClasses`：classType/startDate/endDate/课程包 state 与逻辑、package 模板加载、create baseData 精简
  - `classes/index.tsx`：CreateClassSheet 三处 props 精简、renderTypeTag（循环/X课时标签）移除、卡片时间统一显示排课时间
- **保留**：types/class.ts ClassType（class-detail 显示兼容）、schedule-form 的 scheduleMode/totalLessons（排课侧承载）。
- 验证：typecheck ✅、tests 36/36 ✅、lint 0 ✅、build 中。

## 团课 28 号渲染异常修复（08-23 01:40）
- **问题**：课表团课 tab 8/28（周五）渲染异常——当天只有 1 条灰色 rest 休息卡（cbs-0117 cls-011 钢琴 16:00 rest 0/4），其他日期 3~4 条正常白色课程卡。
- **根因（mock 数据）**：cls-011 钢琴排课日=周二/周四，但 mock 给其铺了 8/23~8/28 连续 6 天，28 号（周五）那条是多余的 rest；而排课日含周五的 cls-004 声乐班（周一/周五 15:00）周五 slot 却缺失（cbs-0001 铺周日、cbs-0002 铺周四）。
- **修复**（`src/data/class-booking.ts`）：① 删除 cbs-0117（cls-011 周五多余 rest）；② 新增 cbs-0012（cls-004 声乐初级班 BASE_DATE+5d 15:00-16:30 active 3/6，与排课日一致）。
- **要点**：CLASS_BOOKING_SLOTS 的日期是 `BASE_DATE=dayjs()` 相对偏移，slot 星期随运行日期漂移——排查"某天渲染异常"要先按 BASE_DATE+偏移算出实际日期，再对排课日 weekdays。
- 验证：typecheck ✅、tests 36/36 ✅、lint 0 ✅、build 中。git 未提交。

## 团课 rest 休息时段不渲染（08-23 02:00）
- **用户口径**：休息（rest）不产生约课，列表**不渲染 rest 卡片**；全天休息的日期自然显示空态；日历也不标记全天休息的日期。
- **改动**：
  - `schedule/index.tsx` renderOpenClassList：`allSlots` 计算时 `.filter((s) => s.status !== 'rest')`；汇总统计移除 summaryRestSlots 及"休息：X 个"文案
  - `data/class-booking.ts` mockGetOpenSlotDates：仅当天存在非 rest 时段才标记为可约日期（全天 rest 日期不再出现在日历）
  - 防御性保留：isRest 灰卡样式与恢复/取消 handler 保留（rest 卡被过滤不会触发，未来直接传入仍正确）
- 效果：7/26、7/27 等单条 rest 日期 → 日历无标记 + 列表空态"当前日期暂无开放预约时段"；8/28 因有 cbs-0012（声乐正常课）正常显示
- 验证：typecheck ✅、tests 36/36 ✅、lint 0 ✅、build 中。git 未提交。

## 课程管理页双滚动条修复（08-23 02:30）
- **问题**：课程管理页（course-management）两个滚动条嵌套——外层整页 `ScrollView scrollY h-screen`（页面级滚动）+ 内层分类 tab `ScrollView scrollX`（横向滚动）互相嵌套；班课/团课/私教分类共用此布局。
- **修复**：外层整页 ScrollView 改为**原生页面滚动**（PageContainer + View，与 classes 等页面一致）：外层 ScrollView 标签 → View（含注释），闭合标签同步；列表容器 pb-[40rpx] → pb-[calc(220rpx+env(safe-area-inset-bottom))]（fixed 新增按钮避让，替代原 ScrollView 的 160rpx pb）；保留分类 tab 横向 ScrollView（必要）。
- **踩坑**：初次编辑把外层闭合误改成 PageContainer 闭合（fixed 按钮/弹窗被挤出）→ TS1128 语法错误；修正为 View 闭合、PageContainer 保持末尾闭合。
- **参照**：classes/permission-settings/audit-log 均为原生滚动（PageContainer+View 无整页 ScrollView）；schedule 等 tabBar 页为 custom 导航 + overflow-hidden 定制布局（已有注释处理，不动）。
- 验证：typecheck ✅、tests 36/36 ✅、lint 0 ✅、build 中。git 未提交。

## 课程管理编辑口径统一 + 移除结课按钮（08-23 02:50）
- **用户口径**：① 课程管理卡片点击统一——已排课→class-detail（保留）；未排课（班级+模板）→统一进"编辑课程页面"（course-form），**移除 class-form 弹窗式编辑入口**；② 班级数据编辑不散乱，统一在 course-form 编辑；③ 移除 class-detail 结课按钮。
- **course-form 班级模式**（type=class）：category 强制 'class' 班课模式；加载 classService.getById+getStudents → fillClassForm（名称/分类/颜色/老师/助教/学员，颜色 ClassColor↔hex 双映射常量提模块级）；保存 classService.update + 学员增删同步（toAdd/toRemove）；删除 classService.remove；隐藏模板专属 UI（时长/人数/科目/课程介绍/图片）；标题"编辑课程"。
- **跳转改造**：course-management 未排课班级 → `course-form?id=&type=class`；class-detail goEdit → 同上（不再跳 class-form）。
- **结课按钮移除**：class-detail 底部"结课"按钮 + handleEndClass/endingClass 全链路清理（useClassDetail 接口/state/返回）；已结课状态展示保留。
- **踩坑**：fillClassForm 需在加载 useEffect 前声明（TS2448）；classService 方法名 removeStudent（非 removeStudentFromClass）；update color 需回映射 ClassColor。
- 验证：typecheck ✅、tests 36/36 ✅、lint 0 ✅、build 中。git 未提交。
