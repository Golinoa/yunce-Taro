# 2026-08-21 工作日志

## 帮助页头部呼吸感优化
- 头部容器 `pt-[24rpx]` → `pt-[48rpx]`（导航栏下方留白加大，解决离导航栏太近问题）

## 帮助页重构：手风琴展开 + 间距优化 + 底部按钮轻量化
- **文件**：`src/package-settings/pages/help/index.tsx`（教师端帮助页）
- **改动内容**：
  1. **手风琴效果**：点击问题行 → 向下内联展开答案（带左侧竖线装饰），再点收起；单开模式（同一时间只展开一项）
  2. **间距优化**：列表项 `py-[28rpx]` → `py-[32rpx]`；整体包在 `bg-card rounded-[24rpx] shadow-card` 卡片容器内；编号加宽 `w-[44rpx]`、字号 `text-[28rpx]`
  3. **底部按钮轻量化**：「去反馈」实心填充按钮 → 统一为「电话客服」+「在线咨询」双按钮，均用 `border border-border/60 bg-card` 轻量风格；图标换为 `mdi-phone-outline` / `mdi-chat-processing-outline`
  4. **新增「展开全部/收起全部」**：列表超过 4 项时显示，点击一键切换
  5. **移除问答详情 Dialog**：不再弹窗展示答案，改为内联手风琴
- 编译验证：✅ 通过（5.84s），产物完整

## 增量编译（删 dist + build:weapp）
- 按用户要求：先删 `dist`，再用 PowerShell 跑 `npm run build:weapp`（带 `VITE_USE_MOCK=true`、`NODE_OPTIONS=--max-old-space-size=4096`）。
- 结果：耗时 ~1m32s，`Compiled successfully`，`postbuild-weapp-fixes` 正常 patch `dist/base.wxml`；产物完整（6 分包 + 主包 app.js/json/wxss、base.wxml、project.config.json，共 11 js / 111 wxml / 81 wxss）。
- ⚠️ 环境现象（非致命）：webpack 文件系统缓存 pack 落盘报 `EPERM: rename ... 0.pack_ -> 0.pack`，缓存包无法持久化。编译仍成功，但跨次增量缓存可能失效、下次偏慢。与已知「文件操作被拦截」同类，属删除/重命名类操作的 EPERM，非代码错误。
- 备注：Taro 启动有 `punycode` DeprecationWarning（node22 弃用，无害）；构建期 `common.js 647KiB` 超建议体积属 Warning。

## 课表日历选中色优化
- **文件**：`src/app.scss`（三处主题作用域：蓝色/珊瑚/橙色）
- **改动**：日历选中圆圈的 3 个 CSS 变量从 `--accent`（强调色）改为 `--primary`（品牌主色）
  - `--schedule-calendar-selected-bg`: `hsl(var(--accent))` → `hsl(var(--primary))`
  - `--schedule-calendar-selected-text`: `hsl(var(--accent-foreground))` → `hsl(var(--primary-foreground))`
  - `--schedule-calendar-dot`: `hsl(var(--accent))` → `hsl(var(--primary))`
- **效果对比**：
  | 主题 | 改前(accent) | 改后(primary) |
  |---|---|---|
  | 蓝色 | #8B5CF6 紫（偏重、和蓝调不搭） | #3B6EF5 蓝（与页面统一） |
  | 珊瑚 | #47C1B6 青绿（和珊瑚不协调） | #f97768 珊瑚红 |
  | 橙色 | #06B6D4 青（和橙冲突） | #FF8A2A 橙 |
- 编译验证：✅ 通过（7.94s）

## ⚠️ 课表日历选中色已回退（用户要求）
- **用户最终决定**：撤销本次日历选中色的所有优化，回到最初原始状态。原因：「联动/Tab 协调等探索效果不行」。
- **回退操作**（`src/app.scss` 三处主题作用域）：
  - `--schedule-calendar-selected-bg`: `hsl(var(--primary))` → **`hsl(var(--accent))`**（还原）
  - `--schedule-calendar-selected-text`: `hsl(var(--primary-foreground))` → **`hsl(var(--accent-foreground))`**（还原）
  - `--schedule-calendar-dot`: `hsl(var(--primary))` → **`hsl(var(--accent))`**（还原）
  - today 相关变量（原本就是 primary）保持不变
  - 注释同步改回「选中态使用强调色(accent)」
- **附带清理**：删除本次探索用的 `calendar-selected-preview.html`（非 app 文件，纯预览草稿）。
- 编译验证：✅ 通过（7.37s）。
- **结论/教训**：用户对「日历选中色 = 主题主色 primary」以及后续「方案 G 联动 Tab/区间」方向均不满意，最终选择保留原始 accent 选中色。后续若再动日历配色，先确认方向再动手，避免反复。

## G 方案最终落地（02:00 后）
- 用户最终确认 **G 方案** 效果（实心主题色圆 + 白色描边 + 浅底描边 Tab），要求迁移进真实代码。
- **改动 4 处**：
  1. `src/app.scss` 三套主题作用域：`--schedule-calendar-selected-bg/-text` 与 `--schedule-calendar-dot` 由 `accent` 改回 `primary`（白字），对齐 G 实心圆。
  2. `uno.config.ts`：新增 `shadow-schedule-selected` Token（`0 6rpx 16rpx hsl(var(--primary)/0.32)`）。
  3. `src/components/CalendarWeekSelector/index.tsx`：周视图 + 月视图选中格加 `border-[4rpx] border-white shadow-schedule-selected`（白色描边 + 投影，G 的「抠出」效果）。
  4. `src/pages/schedule/index.tsx`：课表主分类 Tab 选中态由 `border-schedule-header bg-schedule-header shadow-md`（实心）改为 `border-primary/55 bg-primary/10` + 文字 `text-primary`（浅底描边 1 week 风格，对齐参考图）。
- 编译验证：✅ 通过（9.13s）。产物确认：`bg-primary_a_10`、`border-primary_a_55`、`shadow-schedule-selected`、`schedule-calendar-selected-bg:hsl(var(--primary))`×3 均生成。
- ⚠️ 验证提示：wxss 中 `/` 转义为 `_a_`（如 `border-primary_a_55{`），grep 需用 `border-primary_a_55` 而非 `border-primary\/55`。
- 探索草稿 `calendar-selected-preview.html` 已重建为仅含 G 方案的预览（保留供用户对照）。

## 团课 tab 补 mock 数据（02:00 后）
- **用户原话**「给团课不上mock数据」→ 澄清为打错字，真实意图是「给团课补上 mock 数据」。
- **根因排查**：课表页团课 tab 渲染的是 `openClassSlots`（`classBookingService.getSlotsByClass`，数据来自 `CLASS_BOOKING_SLOTS`）。默认 mock 教师 `teacher1`→`teacher-001`（张老师），其团课班 `cls-011` 虽有但**所有开放名额 slot 日期都硬编码在 2026-06/07**（含 `BASE_DATE=dayjs('2026-06-22')` 与 cbs-0112 的 `2026-07-29`），而 `selectedDate` 默认是今天（2026-08-21）→ 默认视图下 `getSlotsByClass` 匹配不到任何 slot → 团课整片空。
- **改动 2 处**：
  1. `src/data/class-booking.ts`：`BASE_DATE` 由 `dayjs('2026-06-22')` 改为 `dayjs()`（锚到当前日期），修根因；并把 `cls-011` 原 cbs-0112 slot 从 `2026-07-29` 挪到 `BASE_DATE`（今天）并改为「已约3/4 可预约」，再新增 cbs-0113~0117（cls-011 周二/周四 16:00-17:00，覆盖今天~+5 天，状态 active/rest/full 混合）；新增 cls-012 的 cbs-0120~0124（吉他弹唱班 17:00-18:30，今天~+4 天）。
  2. `src/data/mock-database.ts`：新增 `cls-012` 团课班（teacher-001 / campus-center / cat-group / scheduleMode open / autoOpenType full / 周一周四 17:00-18:30），与 cls-011 一起归属默认教师，使团课 tab 显示 2 个班级 + 可预约时段。
- 编译验证：✅ 通过（7.69s）。产物确认 `cls-011`/`cls-012`/`cbs-0112`/`cbs-0120` 均进入 `dist/common.js`，旧硬编码 `2026-07-29` 已从产物移除。
- **提醒**：本次仅把团课名额锚到「今天」~+5 天，过几天后这些 slot 会变过去、团课又会空；如需长期稳定，应让 mock 名额按「相对今天」动态生成（如 `dayjs().add(n,'day')`）。teacher-002（李老师）的 cls-004/005 名额也因 BASE_DATE 改为 dayjs() 一并挪到本周，登录李老师账号也能看到团课了。

## 课表页 + 排课表单页 UI 改造
- **需求**：班课/团课 tab 都要有「排课」悬浮按钮（带文字）；进入排课表单页后，参考截图重新设计，并**选中班级后同步显示关键信息**（扣课时、老师、学员/人数），且班课≠团课逻辑不同（班课=课程内容人员已定、排课决定何时上；团课=设置开放时段等预约）。
- **改动**：
  1. `src/pages/schedule/index.tsx`：悬浮按钮条件由仅 `class/private` 改为 `class/group/private` 全显示；按钮加「排课」文字（`mdi-plus` + Text）；`handleCreateSchedule(sourceMode?)` 接收来源模式并拼到 URL `?sourceMode=`。group 点击传 `activeTab?.mode`（group）。
  2. `src/package-course/pages/schedule-form/index.tsx`：**完整重写**。结构：标题 + 副文案（按 sourceMode 区分班课/团课提示）→ ① 班级名称 Picker → ② 选中班级信息卡片（同步显示：人数、已扣课时 x/总、授课老师、助教、校区、课时单价）→ ③ 排课规则（规则排课/自由排课）→ ④ 开始日期（CalendarMonthSheet）→ ⑤ 重复方式 → ⑥ 上课周几多选 → ⑦ 上课时间多组 +/- + 常用时间段 → ⑧ 自动签到开关 → 上课地点（校区/教室）。保留编辑/调课分支逻辑（mode student/class、selectedDateValue、temporaryRescheduleService 等）。
- 编译验证：✅ 通过（13.19s）。`dist/package-course/pages/schedule-form/index.{wxml,wxss}` 与 `dist/pages/schedule/index.wxml` 均已生成。
- **注意/未决**：排课表单的「保存」目前仍走原有 scheduleService.create/update（班课保存 class_id + day_of_week + 时间；团课场景下 `sourceMode` 仅用于 UI 文案/信息展示差异化，尚未做「开放预约时段」专属提交逻辑——若团课需真的写入开放名额表，需后续接 classBookingService）。本次聚焦 UI 与班级信息同步，未改提交语义。

## 排课页白屏 Bug 修复 + 团课排课按参考图重设计
- **Bug 修复**：`ReferenceError: selectedAssistantTeacher is not defined` → 第 329 行 `rescheduleSummaryTeacherText` 引用了未定义的 `selectedAssistantTeacher`（应为 `selectedAssistantTeacherId` 对应的 computed 变量）。修复：在 `selectedTeachingTeacher` 旁新增 `const selectedAssistantTeacher = useMemo(() => teachers.find(...), [selectedAssistantTeacherId, teachers])`。
- **团课排课模式重设计**（参考图：课程类型/名称/老师/助教/时长/难度/教室/时间+加号卡片）：
  - 排课表单现在根据 `sourceMode=group|class` 走两套完全不同的 UI：
    - **团课（group）**：单个大卡片内含 8 行 FormRow → 课程类型(Picker) → 课程名称(班级Picker) → 老师(Picker) → 助教(Picker) → 课程时长(分,必填) → 课程难度(标签式: 所有人/初级/中级/高级) → 上课教室(校区+教室联动) → 上课时间(虚线框 + 居中橙色加号圆 + 「添加上课时间」文字，点击后展开时间组)。
    - **班课（class）**：保留原有设计（标题→班级名称→信息卡片→排课规则→开始日期→重复方式→周几多选→时间多组→常用时段→自动签到→地点）。
  - 新增状态变量：`courseType`、`durationMinutes`、`difficulty`；新增常量：`COURSE_TYPE_OPTIONS`(8 种)、`DIFFICULTY_OPTIONS`(4 级)；新增内部组件 `FormRow`（复用表单行样式）。
- 编译验证：✅ 通过（5.97s），零报错。产物 `dist/package-course/pages/schedule-form/index.{wxml,wxss}` 完整。

## 团课排课 UI 参考图重构 (02:17)
- 按参考图（Clipboard_Screenshot.png）完全重构团课模式 UI：
  - 单大白卡片包裹所有表单项，每行 `px-[32rpx] py-[32rpx]` + `border-b` 分隔线
  - 统一 label 左 + picker 值右 + chevron-right 箭头
  - 课程难度改为 inline 橙色描边标签（`border-[2rpx] border-primary`）
  - 上课时间独立卡片，虚线框 + 浅灰底（`bg-muted/60`），居中 **#FF8A2A 橙色圆形加号**（88rpx）
  - 移除页面标题区 / 副标题文字
- 编译验证：✅ 通过（6.17s）

## 排课表单统一页面重构（班课/团课合并）(02:37-02:44)
- **需求**：两个类型共用一个页面。字段精简为：课程类型（班课/团课）→ 课程名称 → 消耗课时（Stepper 加减）→ 上课时间；老师/助教/时长/难度 不再作为编辑字段，选中课程后自动带出，作为「课程信息」只读卡片展示在**上课时间下方**。
- **改动**：
  1. `src/package-course/pages/schedule-form/index.tsx`：
     - 删除 班课/团课 两套分支 UI（约 500 行），合并为统一表单（单卡片+分隔行+箭头，沿用参考图风格）
     - `COURSE_TYPE_OPTIONS`（8 种科目）→ `SCHEDULE_TYPE_OPTIONS` = ['班课','团课']；`courseType` state → `scheduleType`（sourceMode 初始化，页面内可切换，`isGroupMode = scheduleType==='group'`）
     - 新增 `consumedHours` state + `<Stepper>`（复用 `@/components/Stepper`，min 0.5 step 0.5）
     - 新增引用信息 computed：`refDurationText`（durationMinutes 或班级 start/end 推算）、`refLevelLabel`（CLASS_LEVEL_LABELS）；`classInfoCard` 提供老师/助教/人数
     - **修复 hooks 顺序 bug**：原团课字段的 useState/useMemo 定义在 loading/loadError/notFound 早退 return **之后**（eslint react-hooks/rules-of-hooks 5 errors）→ 全部上移到 state 区/计算属性区（早退之前）
     - 清理 20+ 未使用定义（SCHEDULE_RULES/REPEAT_OPTIONS/COMMON_TIME_RANGES/DAY_VALUES/DAY_LABELS/scheduleRule/repeatMethod/autoCheckIn/DIFFICULTY_OPTIONS/FormRow/leadTeacherOptions/assistantTeacherOptions/selectedTeachingTeacher/selectedAssistantTeacher/handleModeChange/toggleDay/pageTitle/handleDelete/setDeleting 等）；`students` state 与 Student/WorkflowHeaderCard/Room import 一并删除
     - prettier CRLF 问题通过 `npx prettier --write` + `eslint --fix` 归零；最终 lint **0 errors**（剩 2 个原有良性 warning）
  2. 批量替换用临时 Python 脚本（锚点 `{isGroupMode ? (` … `)}`，CRLF 感知），用完即删
- 编译验证：✅ 通过（6.52s，PowerShell+VITE_USE_MOCK=true）。产物 `dist/package-course/pages/schedule-form/index.js` 确认含全部新文案（消耗课时/课程类型/班课/团课/课程信息等 9 项）。清理 dist_bak_* ×11
- **注意/未决**：①统一表单的「保存」仍走 scheduleService.create/update（班课语义，默认周一/今天）；团课「开放时段写名额」仍未接 classBookingService。②原班课的排课规则/开始日期/周几多选/自动签到/地点 字段在统一 UI 中不再编辑（保留默认值参与校验）。③mock 团课 slot 仍锚定今天~+5 天，会过期变空

## 课程信息卡片重构 + 学员列表拉取 (02:51-02:54)
- **需求**：①「课程信息」沿用主表单风格（单白卡片+分隔行）只读展示；②把该课程的学员列表拉进页面显示（参考 class-form/class-detail 的学员展示）。
- **改动**（`src/package-course/pages/schedule-form/index.tsx`）：
  1. 「课程信息」卡片由浅色描边卡片（`border-primary/15 bg-primary/[3%]`）改为 **`bg-card rounded-[20rpx]` 白卡片 + `border-b border-border/60` 分隔行**（与主表单完全同风格）：标题行（课程信息 + N人 badge）→ 老师/助教/课程时长/课程难度 四行只读 → 学员区块
  2. 学员区块：label「学员」+ N人 → 学员列表（`<Avatar name avatarUrl size="sm">` + 姓名 + 手机号，纵向分隔行 `border-border/40`，空态「暂无学员」）
  3. 新增 `classStudents` state + useEffect（classId 变化 → `classService.getStudents(classId)`，`.catch` 置空）；重新 import `Avatar`、`Student` type（之前清理时删过，本次需要）
- 编译验证：✅ 通过（6.09s）。产物 `index.js` 确认含 getStudents/Avatar/学员(\u5b66\u5458)/消耗课时/课程信息；清理 dist_bak_*（11 个）
- 验证提示：产物中文是 `\uXXXX` 转义，用 node `s.includes('\\u5b66\\u5458')` 验证最可靠（bash grep 转义层数容易误判）

## 全局弹窗选择器统一为 PickerSheet（03:05-03:20）
- **需求**：以「编辑课程页面 course-form」的「所属分类」选择器 `PickerSheet`（`@/components/PickerSheet`，BottomSheet+PickerView 滚轮）为标准，全局统一弹窗选择器，组件抽离复用、不散落代码。
- **盘点**：标准组件 PickerSheet 已有 9 页在用；另有 **6 个页面 12 处**用原生 `<Picker mode="selector">`（微信系统底部弹层，样式不统一）：
  | 页面 | 处数 | 字段 |
  |---|---|---|
  | schedule-form | 2 | 课程类型、课程名称 |
  | leave-request | 1 | 选择孩子 |
  | store-entry | 1 | 门店类型（FormSelect onClick） |
  | lesson-edit | 4 | 上课老师、助教、校区、教室 |
  | lesson-form | 3 | 主讲老师、校区、教室 |
  | profile-edit | 3 | 性别、孩子关系、孩子性别 |
- **统一模式**：多字段页面用「一个 `selector {visible, type}` state + 末尾一个 PickerSheet 按 type 分派 options/value/onConfirm」集中配置（对齐 course-form 的 pickerConfig 最佳实践）；单字段页面用单 visible state。
- **替换后清理**：删除全部残留 computed（classPickerData/selectedClassIdx、campusPickerOptions/campusIndex/roomOptions/roomIndex、selectedTeachingTeacherIndex、teacherIndex 保留、profileGenderIndex/childGenderIndex、GENDER_PICKER_RANGE、childNames）。
- 编译验证：✅ 通过（9.88s）。6 文件 lint 全 0 error/0 unused；base.wxml 含 picker-view×4/picker-view-column×2（PickerSheet 模板全局生效）；清理 dist_bak_*
- **范围外**：`mode="date"/"time"`（日期/时间）与 `mode="region"`（地区）不在本次统一范围（项目另有 DatePickerSheet/TimePickerSheet 体系，且 picker-view 模板已在 base.wxml 复用）

## 微信开发者工具 ENOENT 报错排查（03:24-03:26）
- **报错**：`ENOENT open 'dist\package-course\sub-common\c6e93232665e5fe63e783b587c2cc847.js'`（预编译 precompile 阶段）
- **排查结论**：**工具预编译缓存过期**，非项目问题。
  1. 先误判为产物缺失（只查了 `dist/package-course/sub-common/`），实为**每个分包有独立 sub-common 目录**（package-course/settings/teacher/lead/student 各一份），缺失的 2 个 chunk 实际存在于 package-settings/package-teacher 的 sub-common
  2. 用 node 按**相对路径**（`require("../../sub-common/xxx.js")` 相对引用者解析）全量校验 dist：**244 个相对引用 0 缺失** → 产物完整
  3. `c6e932...` 是旧编译的 chunk 名，本次产物已无；工具 precompile 拿旧清单读新 dist → ENOENT
- **处理**：清 webpack 缓存全量重编（39.34s）确认产物完整后，判定需**在微信开发者工具清缓存**（工具→清除缓存→清除全部缓存，或编译下拉"清缓存并重新编译"，或重启工具）
- **经验**：验证分包产物缺失必须用相对路径解析（node fs.existsSync(path.resolve(dirname, rel))），不能只看单一分包目录；微信工具 ENOENT 优先怀疑工具预编译缓存，先验证 dist 完整再让用户清工具缓存

## 排课页重构：课程名称不默认选 + 抽离 3 个课程组件复用（03:28-03:42）
- **需求**：
  1. 课程名称不默认选择（之前 loadFormData 会 `setClassId(clsList[0].id)`）
  2. 复用「新建课程页面」course-form 的「课程展示」「班课信息」「上课学员」组件，不要在排课页重新写（样式不统一）
  3. 这些数据可编辑，**实时同步到课程管理**（classService）：移除学员/换老师等都通过 classService 写入
- **抽离 3 个组件**（`src/components/course/`）：
  - `CourseDisplayCard.tsx`：课程颜色/年龄组/课程难度（Card+FormRow 分隔行；颜色展示块含 `?` 提示；难度用 primary 描边标签）
  - `ClassTeachersCard.tsx`：授课老师/助教（Card+FormRow 双行；onPick* 回调打开 PickerSheet）
  - `ClassStudentsCard.tsx`：上课学员网格（4 列 + 头像 + 课时；右上角红×移除确认；空态虚线框加号；内部复用 `StudentMultiSelectSheet` 弹窗）
- **schedule-form 集成**：
  - `loadFormData` 删 `setClassId(clsList[0].id)`，课程名称默认空
  - `selectedClass && <>` 内用 3 个 Card 替换原"课程信息"白卡片（移除原老师/助教/时长/难度/学员行 JSX）
  - 新增 state `teacherPickerVisible`/`assistantPickerVisible` + 2 个 PickerSheet（已沿用 PickerSheet 标准组件）
  - `handleStudentsChange(newIds)`：diff 旧新 ID → `classService.removeStudent/addStudents` → 重拉该班学员 → `invalidateStudents(currentUserId)` 刷新 store → toast「已同步到课程管理」
  - `handleTeacherConfirm`/`handleAssistantConfirm`：更新本地 state（前端调整，不立即写 classService，老师/助教改后排课时用新值——用户原话"没有问题"指前端可调即生效；如需严格持久化可后续加 `classService.update`）
  - 删未用的 `refDurationText`、Avatar import
- **Teacher/Assistant 选项**：schedule-form 原本没有 `leadTeacherOptions`/`assistantTeacherOptions` computed，PickerSheet options 直接 inline filter `teachers`（含 role 过滤）
- 编译验证：✅ 通过（7.00s）。4 文件 lint 全 0 error；产物验证：上课学员/授课老师/课程颜色/年龄组/课程难度/已同步到课程管理/点击此处添加/移除学员/从学员库中 全部命中；grid-cols-4 网格样式进产物；Avatar/Card/PickerSheet 已打包
- **未替换**：course-form 内部原内联 JSX 暂未替换为新组件（course-form 仍可工作，与排课页共用 3 个组件是后续迭代项；当前 course-form 与新组件 API 略有差异如 class-form 的 `selectedStudents` 用了 ClassStudentInfo，schedule-form 的 classStudents 用了 Student 类型，类型统一需后续整理）
- **样式统一目标**：schedule-form 班级信息展示从原"白卡片+分隔行"改为 Card+FormRow 风格（与 course-form 统一）

## Bug 修复：抽离组件后 students is not defined（04:30）
- **报错**：选课程时 `ReferenceError: students is not defined`（package-course 分包）
- **根因**：抽离 ClassStudentsCard 传 `allStudents={students}`，但 `students` state 早前清理 unused 时被删（当时无 UI 使用）；未删的引用只在运行时暴露
- **修复**：`schedule-form` 重新加回 `const [students, setStudents] = useState<Student[]>([])`（全部学员池）+ `loadFormData` 里 `setStudents(stuList)`；顺带清 2 个新 unused（`getDurationHours`、`durationMinutes`——随 refDurationText 删除失去引用）
- **教训**：抽离组件引用数据源前先 grep 确认被引用变量仍存在；"编译通过≠运行时 OK"，未定义的 state 引用只在运行时暴露

## 学员卡片 UI 修复 + 弹窗嵌套修复 + 科目筛选缺失修复（14:08-14:32）
- **需求**：①学员头像离小字太近 ②头像缩小一点 ③添加圆形与头像一致 ④添加文字与名字字号一致 ⑤学员选择弹窗"只能看不能点"缺失 UI 全面检查 ⑥弹窗不显示科目
- **修复**：
  1. **弹窗点击失效根因**：`StudentMultiSelectSheet` 内层用 `ScrollView flex-1` 管理列表，但外层 `BottomSheet` 默认 `scrollable=true` 又包一层 ScrollView → **嵌套 ScrollView 导致内层点击/滚动失效**。修复：给 BottomSheet 传 `scrollable={false}`（StudentMultiSelectSheet 是 fillHeight + 内部 flex 布局，本就不需要外层滚动）
  2. **Avatar 新增 `mlg` 尺寸（72rpx）**：介于 md(68)/lg(80) 之间，网格密集场景使用；ClassStudentsCard 学员头像改 `size="mlg"`，移除按钮 36rpx 不变
  3. **添加圆形 80rpx→72rpx**（与头像 mlg 一致）、加号 icon 42→36；**空态圆形 80→72**；**空态文字 24rpx→26rpx**（与学员名字一致）；网格上方描述 `mb-[32rpx]→mb-[40rpx]`（头像与小字拉开间距）；"添加"文字从 `text-primary` 改 `text-foreground` 与名字同色系、26rpx 一致
  4. **弹窗不显示科目**：schedule-form 未加载科目 → 新增 `import { subjectService } from '@/services/campus'` + `subjects` state + `subjectService.getList()` 加载；ClassStudentsCard 传 `subjects={subjects}` + `subjectId={selectedClass?.subject_id}` → 弹窗科目筛选器（全部科目+各科目标签）正常显示
- 编译验证：✅ 通过（8.10s）；lint 0 error；清理 dist_bak_*
- **经验**：BottomSheet 默认 scrollable=true 会在外层包 ScrollView，内部再用 ScrollView 的组件（如 StudentMultiSelectSheet）必须传 scrollable={false}，否则内层列表不可点/不可滚

## Mock 数据文案调整（22:48）
- **张校长 → 万老师**：`src/data/mock-database.ts`（104 contactName 校区联系人 + 384 principal 用户 name）
- **中心校区 → 曦绘艺术**：5 个 mock 文件全部显示文本（`mock-database.ts` name/intro、`lead.ts` campus_name/campusName ×12+、`my-course.ts`、`statistics.ts`、`teacher.ts`；含注释一并替换）
- **会员开通状态**：`src/pages/profile/index.tsx` `isMembershipActive` `useState(false)` → `useState(true)`，会员卡片从「立即开通」变为已开通态（「已开通会员，尊享全部教务特权」「立即查看」「有效期至 2026-12-31」）
- 编译验证：✅ 通过（30.19s，首次全量）。产物验证：万老师/曦绘艺术 → common.js；立即查看/已开通会员 → pages/profile/index.js；清理 dist_bak_*
- **注意**：Write 临时脚本在沙箱下写入后 bash 找不到（沙箱文件视图不一致），改用 node -e 内联批量替换更可靠

## 用户头像支持相册上传（23:05）
- **需求**：profile-edit 头像弹窗原本只支持填 URL（"当前版本暂不支持直接拍照/相册上传"），学习 course-form 封面上传实现，让用户自定义从相册传图
- **复用**：`@/components/ImageUploader` 通用上传组件（封装 `chooseImageTemp`：chooseMedia 相册/拍照 → wx.cropImage 裁剪 → 持久化 USER_DATA_PATH 稳定路径 → 预览/删除），course-form 课程封面用的就是它
- **改动**（`src/pages/profile-edit/index.tsx`）：头像弹窗接入 ImageUploader（`cropScale="1:1"` 方图裁剪、`maxSizeMB=5`），替换"暂不支持"说明 → 「从相册选择或拍照，自动裁剪为 1:1 方图」；保留 URL 输入作兜底 + 分隔线；「默认头像」「确认」按钮不变
- **保存**：`handleAvatarConfirm` 走 `updateField('avatar_url', ...)`，mock 下 ImageUploader 返回的持久化本地路径跨会话有效（与 course-form 封面行为一致；真实联调再补 uploadService 上传到七牛）
- 编译验证：✅ 通过（6.22s）；产物验证：从相册选择或拍照/上传头像/也可以直接填写图片地址 均命中，"暂不支持直接拍照"已移除；清理 dist_bak_*

## 子女头像上传升级（23:17）
- **背景**：个人头像已接 ImageUploader 后，用户提醒检查子女头像——发现添加子女弹窗仍在用旧 `Taro.chooseImage`（临时路径不可靠、无 1:1 裁剪、无大小限制、无删除）
- **改动**（`src/pages/profile-edit/index.tsx`）：
  - import `chooseImageTemp/deleteTempImage/isImageCancelError/isTempImagePath` from `@/utils/image-upload`
  - 新增 `handleChildAvatarPick`：`chooseImageTemp({ maxSizeMB: 5, cropScale: '1:1' })`（相册/拍照 + 1:1 裁剪 + USER_DATA_PATH 持久化 + 超限弹窗），替换旧文件时先 `deleteTempImage`
  - 新增 `handleChildAvatarClick`：未选→直接选图；已选→ `showActionSheet`（重新选择/删除头像）
  - FieldRow 头像行 `onClick` 从内联 `Taro.chooseImage` 改为 `handleChildAvatarClick`
- 编译验证：✅ 通过（37.16s）；产物验证：重新选择/删除头像/选择图片失败 均命中；清理 dist_bak_*

## 图片上传统一：抽离 CourseImageUploader + 门店图片改造（00:25）
- **需求**：门店管理（campus-settings）上传 Logo 和门店背景的两个上传框不好看；把新建课程页面的上传框抽离复用；全面检查其他上传点，统一设计语言（大图用大、头像/小图用小）
- **盘点结论**：多图场景（venue-form 场地照片、teacher-form 宣传图、lesson-form 作业图）用 ImageUploaderList/自绘多图+uploadService 合理保留；单图/大图场景统一用 CourseImageUploader
- **① 抽离 `CourseImageUploader`** → `src/components/CourseImageUploader/index.tsx`（从 course-form 页内搬出）：layout=fullWidth（320rpx 整宽、16:9 裁剪）| square（默认 200rpx、1:1 裁剪）；中央+号主副文案品牌主色；预览 aspectFill+右上删除；点击 previewImage；选图前后滚动位置保护（scrollTopRef）
- **② course-form**：删页内 152 行定义，改 import 复用；清理 unused（Image、chooseImageTemp/deleteTempImage/isImageCancelError，保留 uploadImage）
- **③ campus-settings 门店图片区改造**：Logo → CourseImageUploader square 160rpx（小图）；首张场馆图作为「门店背景图」fullWidth 320rpx 大图（16:9）；其余场馆图 square 200rpx 小图网格（最多 5 张，删除背景图=shift 首张）；ImageUploader import 替换为 CourseImageUploader
- 编译验证：✅ 通过（8.39s）；3 文件 lint 0 error；产物验证：门店背景图/更多场馆图片/上传背景图 命中，cropScale 1:1/16:9 裁剪逻辑在分包 sub-common（course/settings 两分包共用一份组件）
- **经验**：Write 临时脚本在 bash 沙箱下偶发不可见，验证产物用单引号包裹 node -e 内联（转义正确）；grep 搜中文产物必须用 `\uXXXX` 转义形式

## 空白页修复 + 头像交互统一（00:46）
- **空白页报错**：`module 'package-settings/sub-common/1294253c...js' is not defined`（campus-settings + course-form 都空白）
- **排查**：先用相对路径校验 dist（246 引用 0 缺失，chunk 文件存在）→ 判定为**微信工具预编译缓存**与新增跨分包共享组件（CourseImageUploader 被 course/settings 两分包共用 → 进各自 sub-common）的产物结构不一致；上一轮增量编译有 EPERM pack 警告
- **修复**：清 webpack 缓存全量重编（36.72s 无 EPERM 警告）+ 产物复验（246/0 + chunk 存在）→ **用户需在工具清缓存重新编译**（同 03:24 模式）
- **头像交互统一（以子女为准）**：`profile-edit`：
  - **个人头像**：删除「更换头像」BottomSheet 弹窗 + ImageUploader + URL 输入 → 改为**点击直接选相册**（chooseImageTemp 1:1）；已选 → ActionSheet「**查看图片/重新选择/删除头像**」（查看图片=previewImage）；头像加相机小角标提示
  - **子女头像**：ActionSheet 增加「查看图片」项（previewImage），与个人头像完全对齐
  - 清理：showAvatarSheet/avatarInput/handleAvatarConfirm/handleAvatarReset/ImageUploader import 全删；BottomSheet 保留（添加子女弹窗用）
- **校区/课程图片点击查看**：CourseImageUploader 已内置 handlePreview（点击 previewImage）✅ 无需改
- 编译验证：✅ 全量重编通过（36.72s）；产物验证：查看图片/重新选择/删除头像 命中、更换头像弹窗已删、mdi-camera 角标；清理 dist_bak_*
- **自查教训**：改动涉及跨分包共享组件时必须清 webpack 缓存全量重编并做相对路径引用校验，否则增量缓存不一致会在微信工具暴露 module not defined

## 移除 TEMP-DIAG 裁剪诊断（01:22）
- **背景**：之前为定位「裁剪后无预览」问题在 `src/utils/image-upload.ts` 加了 `TEMP-DIAG` 诊断块（含 `diag: string[]` 收集 + `showCropDiagIfNeeded` 弹窗函数 `showModal({title:'裁剪诊断(临时)'})`，注释明确"问题解决后整段移除"），问题早已解决，现在按要求移除
- **清理**：删 297-298 数组声明、5 处 `diag.push(...)`、6 处 `showCropDiagIfNeeded(diag)` 调用、整段 `showCropDiagIfNeeded` 函数定义及 JSDoc 注释
- **坑**：最初正则 `diag\.push\([^)]*\)` 未匹配 3 处带嵌套括号的 push（`'首次校验失败,重试后可加载(落盘延迟)'` 等），改为 `diag\.push\([\s\S]*?\);` 才清干净
- 编译验证：✅ 通过（6.86s）；产物验证：诊断文案 `u88c1u526au8bcau65ad`（裁剪诊断）残留 0；主流程完整（chooseMedia/cropImage API 调用 1 处，showModal 用于图片过大等场景保留）；清理 dist_bak_*

## 排课页信息布局优化（01:53）
- **需求**：①上课学员组件学员列表与上方描述文字太近，留 padding ②移除课程颜色/年龄组/课程难度展示 ③授课老师/助教放到消耗课时上面，信息不分割
- **改动**（`schedule-form`）：
  - 主表单卡片顺序改为：课程类型 → 课程名称 → **[选中课程后] 授课老师 → 助教** → 消耗课时（老师/助教用分隔行样式与课程信息同卡片，点击 PickerSheet 可调，逻辑不变）
  - 删除班级信息区的 ClassTeachersCard + CourseDisplayCard，只留 ClassStudentsCard（上课学员）
  - 清理 unused：ClassTeachersCard/CourseDisplayCard import、refLevelLabel、CLASS_LEVEL_LABELS import
- **ClassStudentsCard**：描述文字 `mb-[40rpx]` → `mb-[48rpx]`（学员列表与上方文字拉开距离）
- 编译验证：✅ 通过（5.89s）；产物验证：授课老师/助教/消耗课时/上课学员 命中；课程展示/课程难度/年龄组 已移除（0）；清理 dist_bak_*
- **注意**：ClassTeachersCard/CourseDisplayCard 组件文件保留（无引用，其他页面可复用）

## 全项目数据流审计（仅报告，未改代码）→ docs/audit-data-flow-report.md（02:14）
- **规模**：115 页 / 22 services / 13 stores / 21 data 文件（中心库 mock-database.ts 3814 行）
- **核心结论：Mock 层双库分裂 + 写回不一致**，这是"拼凑感"根源：
  1. **双库/三库**：teacher.ts（教师+薪资）、member-card.ts（会员卡）、course-template.ts、statistics/data-center（统计）、lead 均为独立库，与中心库互不相通；教师实体双份
  2. **假增删改**：create 大多 push（生效），但 update 只 return 新对象不写回、delete return true、扣课时只 find 不扣（mockUpdateStudent/mockCreateSchedule/mockCreateClass/mockDeductPackageHours 已核）
  3. **五条断链**：消课→扣课时✗ / 排课→课表✗ / 办卡→学员课时✗ / 薪资←课时✗ 统计第三套✗ / 更新删除✗
  4. **store 覆盖 21/115 页（18%）**；useAuth 49/115（43%）
- **报告含**：规模画像、数据源架构图、5 大发现（附证据）、11 条业务流打通度表、10 项检测目标（两页面对比即可复现）、10 条验收标准、4 阶段整改路线（A 止血统一写回 → B 打通联动 → C store 收敛 → D 后端切换）
- 后续实施前需用户确认；修复时以"单一数据源+写必回写+跨库联动"为验收底线

## 数据流修复实施（02:20-13:10，5 轮提交，每轮前已 git 备份）
- **af18135 阶段A止血**：学员/排课/班级/课包真实增删改（update 写回数组、delete splice、扣课时真实扣减）；顺带全量 typecheck 0 错误（历史遗留：Avatar mlg、Room import、selectedClass 顺序、Class.pricePerLesson、CampusType 'main'、多处 unused）
- **54605cc 阶段B1**：mockCreateLessonRecord 模拟后端自动扣减课包（mapLessonRecordInput 保留 packageId）；次卡发卡同步建课包（mockIssueMemberCard import mock-database）；次卡次数调整差额同步课包；新增 invalidatePackagesCache 修复 studentServicePackagesCache 永不过期
- **adfe2a1 阶段B2+B3**：线索转正自动创建正式学员（TS-→stu-，child_name→name 等）；statistics-base 四实体由 mock-database 派生（amount=课时×班级单价、feeAmount=课包聚合、monthlySalary=薪资记录聚合），-961 行静态数据
- **d1f9067 通知**：mockSendNotification 写回 NOTIFICATIONS；已读标记真实修改
- **复核自洽（无需修）**：请假审批写回、教师薪资 _teachers map 写回（之前审计误判 return true）、教师 ID 双库同命名空间、临时调课有 mock 分支
- **待办**：约课→课时联动（class-booking 独立库）、store 收敛（阶段C，115页仅21用 store）、教师双库合并、运行时复验
- **验证流程固化**：改数据层 → prettier+tsc+eslint（0错）→ mock 编译 → 相对路径引用校验（246/0）→ 清理 dist_bak → git 提交
- **注意**：EPERM pack 缓存警告偶发，涉及跨分包共享组件/大改动时清 webpack 缓存全量重编

## 教师双库合并 + 约课口径（14:02）
- **教师双库成因**：中心库 mock-database.TEACHERS（关联层：学员/班级/排课/统计 9 文件按 teacherId 匹配）vs 管理库 data/teacher._teachers（教师 CRUD + 薪资扩展），历史演进互不写回；**同名 ID 数据不同**（teacher-001 中心库=张老师、管理库=王校长），管理库缺 userId/campusIds
- **合并方案（3dbf99f）**：管理库为唯一可写源，中心库 TEACHERS 实时派生视图：BASE_TEACHERS（保留 userId/campusIds 关联字段）+ 管理库同名覆盖（name/phone/role/status/subjects）+ 管理库新增教师自动并入（userId=`user-${id}`）；data/teacher 导出 getManagedTeachers()，mockAddTeacher/mockUpdateTeacher/mockResignTeacher 后 syncTeacherView() 原位 splice 刷新（引用不变，9 个引用文件零改动）
- **ESM 循环依赖处理（关键坑）**：mock-database ↔ data/teacher 互 import → 用 **var 声明（无 TDZ）+ getter 兜底返回 [] + 模块求值后 setTimeout(0) 首刷**，两种加载顺序均安全；**不能用 require 动态加载（小程序运行时不可用）**
- **约课口径**：用户确认「签到才扣次」→ 复核 class-booking：约课确认只写预约记录不扣课时、自动开班生成 hours=0 占位课次、实际扣减在消课/签到（mockCreateLessonRecord 按 hours_used 扣）→ **已符合口径无需改动**
- 全量重编 30.4s 无警告；typecheck/lint 0；产物 246/0
